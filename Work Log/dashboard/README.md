# Work Log Dashboard

This folder contains a generated, self-contained dashboard built from `worklog-index.csv` and the Markdown records.

Rebuild it with:

```powershell
python <skill-dir>/scripts/worklog.py dashboard --project-root .
```
