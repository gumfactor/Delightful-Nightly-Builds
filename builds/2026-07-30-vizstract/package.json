{
  "name": "vizstract",
  "version": "1.0.0",
  "description": "Visual abstract generator for research studies — a deterministic SVG layout engine with optional AI field extraction",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1"
  }
}
