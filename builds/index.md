# Nightly Builds — Catalog

> **Claude:** Append one row to the Full Catalog table each night. Update the Stats block and Last 7 Builds section.
> Never delete or rewrite existing rows — this is an append-only record.
> **You:** Add `Your Rating` (1–10) and `Rating Notes` after reviewing a build. Both feed future build decisions.

---

## Stats

- **Total builds:** 83
- **Completed:** 80
- **Partial:** 0
- **Aborted:** 0
- **Discarded:** 3
- **Last build date:** 2026-09-05
- **Categories used (all time):** A, B, C, D, E, F, G, H, I

---

## Last 7 Builds (Quick Reference for Decision-Making)

- 2026-08-29 — [G] — ambitious — Zebra Lab (complete)
- 2026-08-30 — [H] — ambitious — Layer Guard (complete)
- 2026-09-01 — [A] — ambitious — Fleet Drift (complete)
- 2026-09-02 — [B] — ambitious — CiteForge (complete)
- 2026-09-03 — [C] — ambitious — Promptbook (complete)
- 2026-09-04 — [D] — ambitious — CaseForge (complete)
- 2026-09-05 — [E] — ambitious — Mediation & Moderation Analysis Lab (complete)

---

## Full Catalog

| Date | Category | Complexity | Title | Short Description | Tech | Status | Your Rating | Rating Notes |
|------|----------|------------|-------|-------------------|------|--------|-------------|--------------|
| 2026-06-06 | B | ambitious | AI Session Context Bridge (ctxlog) | Python CLI to capture AI coding session state and generate markdown handoff documents | Python 3, stdlib, pytest | complete | 3 | Requires manual entry to be useful — value depends entirely on what you write into it, making it no better than a markdown file. Architecture is sound but the wrong layer was automated. Would score higher with auto-capture of git state and Claude Code session transcript. |
| 2026-06-07 | H | focused | Git Standup Reporter | Python CLI to summarise recent git commits as a standup report; extended to pull GitHub pushed commits and local unpushed commits automatically | Python 3, stdlib, pytest | complete | — | — |
| 2026-06-08 | F | focused | Quick Data Profiler | Python CLI that profiles CSV and JSON/JSONL files — infers column types, shows null rates, computes numeric distributions, and lists top-value frequencies | Python 3, stdlib, pytest | discarded | 1 | Totally redundant with pandas df.describe() and R summary() — trivially handled by existing tools in the user's stack. No reason to build this. |
| 2026-06-09 | A | focused | Investment Research Notes | Browser app for capturing investment thesis notes per ticker with conviction rating, status lifecycle, and JSON export — localStorage only, no live data | HTML/CSS/JS, Playwright | discarded | 2 | Good field design (conviction rating, watchlist/owned/passed lifecycle) but localStorage only with no real data integration makes it hollow. The right ideas in the wrong shell. |
| 2026-06-10 | A | ambitious | Investment Research Platform | Python CLI combining live watchlist metrics (prices, sparklines, 52W range, P/E, market cap) with a persistent thesis journal — per-ticker notes with price-at-note capture, % move since, and full CLI for add/show/list/search/delete | Python 3, yfinance, pytest | complete | — | — |
| 2026-06-12 | A | solid | Investment Watchlist Dashboard | Python CLI that fetches yfinance data for a watchlist and renders an HTML dashboard with 52-week progress bars, dark/light mode, and terminal text output | Python 3, yfinance, pytest | discarded | 3 | Near-duplicate of the Jun 10 build — same data sources, similar output, marginal rendering differences. 52-week progress bar and --text mode are slightly better than Jun 10 equivalents but not enough to justify keeping both. |
| 2026-06-14 | C | focused | Investment Thesis Journal | Python CLI to record investment research notes per ticker with live price capture at time of writing for later accountability | Python 3, yfinance, pytest | complete | 4 | Good core concept — price-at-time-of-note creates genuine accountability over time. Delivered as a bare CLI with no view layer; value is real but half-realized. Needs dashboard integration to be worth reaching for daily. |
| 2026-06-17 | F | ambitious | Qualtrics Survey Data Inspector | Python CLI that parses Qualtrics CSV exports (3-row header format), computes research-quality data QC metrics (completion, timing, missing data, straight-lining, duplicate IPs, Cronbach's alpha), and produces a text + HTML report plus a cleaned CSV with QI_Flags column | Python 3, stdlib, pytest | complete | 9 | — |
| 2026-06-18 | G | ambitious | Regex Dojo | Browser puzzle game with 20 progressive levels — write a regex to match the green strings and reject the red ones; teaches literals through lookaheads with real-time feedback and localStorage progress | Vanilla HTML/CSS/JS, Playwright | complete | — | — |
| 2026-06-19 | H | ambitious | dep-check: Python Dependency Auditor | Python CLI that audits requirements.txt/setup.cfg/Pipfile against PyPI — classifies each pinned package as up-to-date/patch/minor/major, flags yanked releases, outputs coloured terminal report or self-contained HTML dashboard; --exit-on-outdated enables CI gating | Python 3, stdlib, pytest | complete | — | — |
| 2026-06-20 | I | solid | Run Planner | Python CLI to log runs and track weekly mileage, with Open-Meteo 7-day weather scoring for running/golf/boating comfort windows and a Chart.js HTML dashboard | Python 3, Open-Meteo, Chart.js, pytest | complete | — | — |
| 2026-06-21 | A | ambitious | GitHub Repository Health Scorecard | Python CLI that fetches all GitHub repos via GITHUB_TOKEN, computes a composite health score from recency/CI/issues, and generates a dark-mode HTML dashboard with Chart.js doughnut chart, sortable/filterable repo table, and optional AI-generated briefing | Python 3, GITHUB_TOKEN, Chart.js, Anthropic API, pytest | complete | 6 | Good idea and the right output format — consolidates repo health info that would otherwise require clicking through GitHub. Loses points for overlapping with GitHub's own Insights views and being very similar to the Jun 11 terminal build. Would score significantly higher if it incorporated AI agent conversation history per repo, giving a view GitHub can't provide. |
| 2026-06-22 | B | ambitious | Morning Briefing | Python CLI combining GitHub activity, yfinance portfolio pulse, Open-Meteo weather windows scored for run/golf/boat, and Claude Haiku AI synthesis into a single daily HTML dashboard and markdown file | Python 3, yfinance, Open-Meteo, GITHUB_TOKEN, Anthropic API, Chart.js, pytest | complete | 5 | Right concept — multi-source daily digest with AI synthesis is genuinely useful. But ChatGPT's scheduling feature achieves the same result with a 2-minute setup, which undercuts the "this build solves something I couldn't otherwise do" argument. The value is real but the build is over-engineered for a use case existing tools cover adequately. |
| 2026-06-23 | C | ambitious | Paper Lens | Python CLI that queries arXiv across 4 topic areas, batches abstracts to Claude Haiku for relevance scoring (1–10) and plain-English summaries, stores results in SQLite with deduplication, and renders a dark-mode HTML inbox with filter tabs, search, and read-state tracking | Python 3, arXiv API, Anthropic API, SQLite, pytest | complete | 6 | Solid concept — AI relevance scoring is the right differentiating layer that turns a raw paper feed into a prioritized inbox. Limited by arXiv-only sourcing; a neuroscience researcher needs PubMed and Google Scholar at minimum. Worth extending rather than discarding — the core pipeline is sound. |
| 2026-06-24 | D | ambitious | AI Lecture Builder | Python CLI using Anthropic API to generate a complete 7-section lecture package (objectives, outline, hook, discussion questions, quiz, key concepts, homework) and render it as a tabbed dark-mode HTML viewer with copy and export functions | Python 3, Anthropic API, pytest | complete | 2 | The AI-generated content is the right core value, but a tabbed HTML viewer adds overhead without adding capability the user can't get from a single Claude prompt. Needs a genuinely differentiating layer: integration with existing course materials, Canvas/LMS export, or a persistent lecture library. As built, a power user replicates this with one prompt in the Claude interface. |
| 2026-06-26 | F | ambitious | GitHub Developer Activity Explorer | Python CLI that fetches all GitHub repos via GITHUB_TOKEN, analyzes commit patterns across 12 months, and renders a dark-mode HTML dashboard with hourly heatmap, day-of-week distribution, 52-week volume trend, repo focus map, streak analytics, and an AI developer profile via Claude Haiku | Python 3, GITHUB_TOKEN, Chart.js 4.4.4, Anthropic API, pytest | complete | — | — |
| 2026-06-27 | G | ambitious | Neurofact | Self-contained browser quiz game: 30 neuroscience claims (15 real research findings, 15 AI-generated plausible fakes) presented one at a time — players press Real Finding or AI Generated, then see the correct answer and explanation; final screen shows grade (A–F), accuracy, streak, and a real-vs-fake breakdown | Vanilla HTML/CSS/JS, Playwright, Python 3, pytest | complete | — | — |
| 2026-06-28 | H | ambitious | ci-pulse: GitHub Actions Performance Analyzer | Python CLI that fetches all completed GitHub Actions runs across every repo via GITHUB_TOKEN, computes per-workflow avg/p95 duration and failure rates, ranks by improvement potential, and generates a dark-mode HTML dashboard with 3 Chart.js charts (slowest workflows, failure rates, 30-day trend) and a sortable per-workflow table; AI insights panel via Claude Haiku when ANTHROPIC_API_KEY is set | Python 3, GITHUB_TOKEN, Chart.js 4.4.4, Anthropic API, pytest | complete | — | — |
| 2026-06-29 | I | ambitious | Project Pulse: Multi-Project Context Manager | Python CLI to register and track multiple simultaneous projects — syncs GitHub commits via GITHUB_TOKEN, records manual activity notes (idempotent), generates AI context briefs via Claude Haiku with text fallback, and renders a dark-mode HTML dashboard with a 30-day stacked bar Chart.js timeline, staleness badges (green/yellow/orange/red), project cards with type/repo tags, and a type filter | Python 3, GITHUB_TOKEN, Chart.js 4.4.4, Anthropic API, SQLite, pytest | complete | — | — |
| 2026-06-30 | A | ambitious | GitHub Developer Analytics Dashboard | Python CLI that fetches all owned repos via GITHUB_TOKEN, collects 12 months of commit history, and renders a dark-mode HTML dashboard with four tabs: Overview (hero stats + top repos bar), Timeline (CSS grid heatmap: repo × month), Rhythm (hour-of-day + weekday bar charts), and Languages (stacked horizontal bar by repo) | Python 3, GITHUB_TOKEN, Chart.js 4.4.4, requests, pytest | complete | — | — |
| 2026-07-01 | B | ambitious | BIDS Dataset Organizer & Validator | Python CLI that scans a neuroimaging dataset directory against core BIDS naming rules (entity order, required JSON sidecars, events.tsv for task runs, dataset_description.json, zero-padding consistency, duplicate detection, session consistency), produces text/JSON/dark-mode HTML reports, safely auto-fixes zero-padding mismatches with a dry-run-by-default `--apply` flag, and generates an optional Claude Haiku plain-English action list from the structural findings | Python 3, stdlib, Anthropic API (optional), pytest | complete | — | — |
| 2026-07-02 | C | ambitious | PubMed Research Radar | Python CLI that queries PubMed E-utilities (free, no auth) across 5 saved topics seeded from forensic/affective neuroscience research interests, dedupes articles by PMID in SQLite, scores relevance 1-10 with a plain-English summary and methodology tag via Claude Haiku (with a deterministic keyword-overlap fallback when no API key is set), and renders a dark-mode HTML radar report with topic tabs, relevance sorting, client-side search, and localStorage star/read state | Python 3, PubMed E-utilities, Anthropic API (optional), SQLite, pytest | complete | — | — |
| 2026-07-03 | D | ambitious | WeatherSong | Browser instrument that fetches live weather (Open-Meteo) for a chosen city and deterministically maps temperature/wind/cloud cover/precipitation/weather code into a native Web Audio synthesis engine (drone oscillator, filter, procedurally generated convolution reverb, scheduled percussion — no external audio library) and a synchronized Canvas 2D generative visual; includes a localStorage Weather Journal that replays a saved day's exact parameters | Vanilla HTML/CSS/JS, native Web Audio + Canvas 2D APIs, Open-Meteo, Playwright | complete | — | — |
| 2026-07-04 | E | ambitious | Power Lab | Interactive statistical power, sample-size, and effect-size trainer — a Power Explorer tab with live overlapping-distribution and power-vs-N charts (hand-drawn Canvas 2D, no charting library), a Sample Size Calculator producing a grant-ready summary sentence, a Cohen's d ↔ Pearson's r ↔ t-statistic effect-size converter, and an 18-scenario "guess the power bucket" quiz whose answers are always derived live from the same power formula (never hardcoded) | Vanilla HTML/CSS/JS (classic scripts, no ES modules so it opens directly via file://), native Canvas 2D, Playwright | complete | — | — |
| 2026-07-05 | F | ambitious | TrialScope: Behavioral & Reaction-Time Data QC Explorer | Python CLI that ingests a trial-level behavioral/RT CSV export (auto-detecting or explicit column mapping), computes per-subject/condition stats, flags anticipatory guessing, robust median/MAD-based RT outliers, chance-level performance (binomial test), incomplete data, and implausible-ceiling performance from configurable live-computed rules, recommends exclusions, and renders a dark-mode interactive HTML report (sortable/searchable subject table, hand-drawn SVG RT histograms and learning curves) plus a cleaned CSV and an AI-or-template "Participants & Data Quality" methods paragraph | Python 3, stdlib only (urllib for the optional Anthropic API call), pytest | complete | — | — |
| 2026-07-06 | G | ambitious | Synapse Sort | Daily Connections-style category-sorting puzzle game with a hand-curated 30-puzzle bank drawn from the user's own interests (neuroscience/psychology, AI & agentic workflows, investing/markets, Canadian business, running/golf/boating) — deterministic daily puzzle by UTC date, archive/practice mode for any puzzle, one-away hints, 4-mistake limit, colorblind-safe difficulty labels, shareable emoji result grid, and localStorage streak/stats tracking | Vanilla HTML/CSS/JS (classic scripts, no ES modules so it opens directly via file://), Playwright | complete | — | — |
| 2026-07-07 | H | ambitious | Schema Sentinel | Python CLI that infers structural schemas from JSON/JSONL/CSV files, diffs two snapshots (or walks one file's entire git history via read-only `git log`/`git show`), and classifies every field-level change as breaking/risky/safe (removed field, incompatible type change, new-vs-lost enum value, required-vs-optional flips) with a colored terminal report, JSON output, a self-contained dark-mode HTML report, `--fail-on` CI gating, and an optional Claude-generated migration summary with a deterministic template fallback | Python 3, stdlib only, Anthropic API (optional, via urllib), pytest | complete | — | — |
| 2026-07-08 | I | ambitious | Ledger Lens | Python CLI that turns a bank/credit-card CSV export (auto-detecting columns, incl. split Debit/Credit) into a categorized spending report — 14-category rule-based classifier with optional Claude enrichment (digit-sequence redaction before any text leaves the machine) for ambiguous merchants, merchant+amount-clustering recurring-charge detection, monthly aggregation, optional budget-vs-actual comparison, and a self-contained dark-mode HTML dashboard (Chart.js donut + trend line, sortable/searchable transaction table, degrades gracefully to table-only if the CDN is unreachable) plus terminal/JSON/cleaned-CSV output modes | Python 3, stdlib only, Anthropic API (optional, via urllib), pytest | complete | — | — |
| 2026-07-09 | A | ambitious | Pipeline Pulse | Python CLI that reconciles this repo's own `builds/index.md` catalog against its actual git history (no GitHub API/token needed) to show which nightly builds have landed on `main` vs. which are still stuck in an open branch/PR, ranked by days waiting — discovered live tonight that ~21 of 28 tracked builds were unmerged; renders a self-contained dark-mode HTML dashboard (Chart.js merged/backlog + rating-trend + category/complexity charts, all with graceful text fallback if the CDN is unreachable, sortable/searchable build table, "needs attention" panel with GitHub compare links) plus a terminal summary and optional Claude-generated briefing | Python 3, stdlib only, Anthropic API (optional, via urllib), pytest | complete | — | — |
| 2026-07-10 | B | ambitious | Worklog: Cross-Agent Project Activity Workstreams | Python CLI (stdlib-only) that automatically collects Git commits/branches/tags and GitHub issues/PRs, plus manually-ingested provider-neutral JSON agent checkpoints (objective, decisions with rationale, unresolved items, next steps, validation results), into a local SQLite event ledger, then deterministically correlates them into "workstreams" (explicit checkpoint hint > issue/PR reference > active branch > touched-file overlap > dated general bucket, each recording its signal and confidence). Ships `sync`, `checkpoint`, `workstreams`, `timeline`, `standup`, `resume` (with HEAD-drift and rebased-checkpoint staleness detection), `why` (decision search), `show-event`, and `search`. Implements the highest-rated backlog idea (9/10, Idea Brief) as a thin-but-complete first release, explicitly built to absorb and fix what the 2026-06-06 AI Session Context Bridge and 2026-06-07 Git Standup Reporter got wrong (manual note-taking, no persistence/correlation) | Python 3, stdlib only, GITHUB_TOKEN (optional), pytest | complete | — | — |
| 2026-07-11 | C | ambitious | Connectome | Python CLI that indexes a folder of the user's own Markdown/text notes into a local knowledge base — deterministic corpus-wide TF-IDF-style concept extraction (with optional Claude Haiku enrichment, gracefully falling back when no API key is set), rarity-weighted note-to-note link scoring, incremental SQLite-backed indexing (unchanged files skipped by content hash), and a self-contained dark-mode HTML knowledge base (search, tag cloud, note detail, hand-drawn Canvas concept graph, safely-escaped note rendering verified live in headless Chromium against script-injection attempts). Every free public API in PROFILE.md's Data Sources except GitHub returned 403 from this session's egress policy, so this build deliberately needs no external network access to work | Python 3, stdlib only, Anthropic API (optional, via urllib), pytest | complete | — | User feedback (2026-07-11, not yet tested): concept is "fantastic" — an Obsidian-like local knowledge graph over the user's own notes is a genuinely wanted product, independent of execution quality. No numeric rating yet since the user hasn't run it. Once tested, revisit and add an execution score; until then, treat the concept validation alone as a positive signal for future Personal Knowledge Tool builds that index the user's own note corpus. |
| 2026-07-12 | D | ambitious | Research Question Forge | Python CLI that combinatorially generates research-question/hypothesis skeletons for the user's own forensic/affective neuroscience domain by crossing a curated taxonomy (population × construct × outcome × method × theoretical frame) through a compatibility rule engine (6,928 valid combinations out of 49,000 possible), scores each for novelty via Jaccard token overlap against everything previously generated, optionally polishes the strongest into grant-ready prose via Claude Haiku (deterministic template fallback with no key), and persists everything in a searchable local SQLite library browsable via a self-contained dark-mode HTML viewer (search/filter/detail panel/Copy-as-Markdown, verified live in headless Chromium) | Python 3, stdlib only, Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-07-13 | E | ambitious | CircuitLab | Interactive browser trainer for the specific brain regions and circuits central to the user's own affective/forensic neuroscience research and teaching (empathy, psychopathy, stress) — two hand-authored clickable SVG diagrams (lateral surface + medial/subcortical cutaway, 13 regions), three quiz modes (Label, Function Match, Circuit Trace across 6 named circuits), a 0–3 Leitner-style local mastery tracker color-coded on the diagram itself, and a Case Vignette mode with 8 curated clinical/research scenarios plus an optional AI-generated-vignette path calling the Anthropic API directly from the browser with a user-supplied, session-only key | Vanilla HTML/CSS/JS (classic scripts, no ES modules so it opens directly via file://), Anthropic API (optional, direct browser call), Playwright | complete | — | — |
| 2026-07-14 | F | ambitious | GrantScope | Python CLI that queries the free NIH RePORTER API (no auth) across 5 saved topics seeded from the user's forensic/affective neuroscience research domain (empathy, psychopathy, stress, forensic neuroscience, affective neuroscience), stores funded projects in local SQLite with dedupe-by-project-number, computes funding-by-year/top-institutes/top-organizations/mechanism-breakdown aggregations plus stdlib keyword extraction, generates an optional Claude Haiku funding-landscape briefing (deterministic template fallback), and renders a self-contained dark-mode HTML dashboard (Chart.js 4.4.4, per-topic tabs, searchable project table, offline text-fallback charts verified live in headless Chromium — a bug in the search-empty-state message was found and fixed during that verification) | Python 3, stdlib only, NIH RePORTER API, Anthropic API (optional, via urllib), SQLite, Chart.js 4.4.4, pytest | complete | — | — |
| 2026-07-15 | G | ambitious | Confound Hunter | Browser game that trains research-methods literacy: 30 hand-authored study vignettes across a 10-item methodological-flaw taxonomy (confound, selection bias, no control group, demand characteristics, ceiling/floor effect, regression to the mean, correlation-causation, underpowered sample, lack of blinding, overgeneralization), grouped into 3 chapters of increasing subtlety with a 70%-accuracy unlock gate; a date-seeded 5-question Daily Challenge with a shareable emoji-grid result and a one-play-per-UTC-day gate; and a per-flaw-type Mastery Dashboard persisted across sessions | Vanilla HTML/CSS/JS (classic scripts, no ES modules so it opens directly via file://), Playwright | complete | — | — |
| 2026-07-16 | H | ambitious | AgentLint | Python CLI (plus a companion Claude Code Skill wrapper) that audits AI agent instruction files (CLAUDE.md/AGENTS.md-style docs) for broken file/anchor references, missing required sections, and a keyword-overlap heuristic for possible "Always X"/"Never Y" contradictions — plus an optional Claude-powered semantic review that cross-checks factual claims in the instructions against a supplied ground-truth data file. Directly motivated by a real bug found during tonight's orientation: this repo's own CLAUDE.md calibration note ("every build has scored 4/10 or below") is now false against this very index.md (Qualtrics scored 9/10) | Python 3, stdlib only, Anthropic API (optional, via urllib), pytest | complete | — | — |
| 2026-07-17 | I | ambitious | Deadline Guardian | Python CLI that tracks recurring academic/research administrative deadlines (IRB/ethics renewals, grant progress reports, conference/manuscript submissions, course prep, student evaluations) in local SQLite, with a `capture` command that extracts structured deadlines (title/category/due date/recurrence) from pasted or piped unstructured text via Claude Haiku, falling back to a deterministic regex/keyword parser when no API key is set; completing a recurring deadline automatically schedules the correctly-dated next occurrence (annual/semesterly/custom-month-interval, with leap-year and month-length edge cases handled); renders a self-contained dark-mode HTML dashboard (urgency-bucketed: Overdue/Due This Week/Due This Month/Upcoming/Completed, category filter chips, all user text inserted via textContent, verified live in headless Chromium against a script-injection attempt) | Python 3, stdlib only, Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-07-18 | A | ambitious | CanEcon Pulse | Python CLI/dashboard that fetches live Canadian macroeconomic indicators — USD/CAD and EUR/CAD exchange rates, the Bank of Canada policy interest rate, national CPI, and the national unemployment rate — from the free, no-auth Bank of Canada Valet API and Statistics Canada Web Data Service, storing deduplicated observations in local SQLite so a real multi-run history accumulates over time; every indicator degrades gracefully (skip + warning) on an individual fetch failure rather than aborting the sync; renders a self-contained dark-mode HTML dashboard (Chart.js 4.4.4, per-indicator trend chart, day/week/month delta badges with a tolerance window so mixed-frequency series are never mislabeled, graceful text-table fallback when the CDN is unreachable — verified live in headless Chromium, where the CDN was in fact blocked and the fallback engaged correctly with zero page errors) plus an optional Claude Haiku "what this means for Canadian consumers / Canadian-made products" briefing with a deterministic template fallback. First build in the catalog to use Canadian government open data (Bank of Canada + StatsCan), tied to PROFILE.md's named "Canadian economic policy" interest and The Canada List project | Python 3, stdlib only, Bank of Canada Valet API, Statistics Canada WDS, Anthropic API (optional, via urllib), SQLite, Chart.js 4.4.4, pytest | complete | — | — |
| 2026-07-19 | B | ambitious | Protocol Forge | Python CLI that turns a structured research-study description into a compliance-checked IRB/ethics protocol draft — a deterministic, AI-free rule engine flags missing required fields, deception without a debrief plan, vulnerable populations lacking safeguard language, identifiable data with no security mention, missing retention periods, undocumented risks, and compensation offered without a withdrawal-without-penalty mention; a local SQLite protocol library lets approved protocols' section text become reusable boilerplate for future tag-similar studies (deterministic tag-overlap matching, not fuzzy/embedding similarity, so it works fully offline); each of 6 canonical sections drafts via a 3-tier fallback (reuse approved boilerplate → optional Claude Haiku draft → deterministic template, always functional with no API key). Targets "Ethics application generation," a friction point named directly in PROFILE.md and never previously built | Python 3, stdlib only, SQLite, Anthropic API (optional, via urllib), pytest | complete | — | — |
| 2026-07-20 | C | ambitious | CanFile: Canadian Ownership Knowledge Cards | Python CLI that looks up a company on the free, no-auth Wikidata API (search + claims for country/headquarters/parent-organization/owned-by, plus one-hop parent/owner country resolution), pulls a Wikipedia summary, and applies a transparent deterministic rule engine — with optional Claude Haiku enrichment, always falling back to the rule-engine text — to produce a confidence-rated Canadian/foreign/uncertain ownership verdict with cited sources; every lookup is stored as a new version in a local SQLite knowledge card rather than overwritten, browsable via a self-contained searchable/filterable dark-mode HTML index. First build to directly target The Canada List, a named active project with zero prior builds; a near-identical idea (backlog #13) was previously abandoned only because the build container's egress proxy returned 403 on wikidata.org — this build follows CLAUDE.md's guidance to design for the user's runtime and mock the network layer in tests instead | Python 3, stdlib only, Wikidata API, Wikipedia REST API, Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-07-21 | D | ambitious | Bridgework | Python CLI that generates analogies bridging the user's own stress/empathy/psychopathy neuroscience research to everyday domains (kitchen stove, storm, thermostat, athletic training, traffic, garden, orchestra, stadium wave, computer network, smoke alarm, dam/reservoir, phone battery) — a 20-concept × 12-domain taxonomy filtered through a 7-type structural-mechanism compatibility rule engine (97 of 240 possible pairs × 3 audience registers = 291 valid triples), deterministic template generation that always works with no key, optional Claude Haiku polish per audience register (undergrad lecture / public talk / book chapter) with unconditional template fallback, Jaccard-novelty-scored and never-overwritten in a persistent local SQLite library, and a self-contained dark-mode HTML viewer (search/filter/Copy-as-Markdown, all entry text delivered as JSON and inserted via textContent/createElement — a live script-injection payload verified inert in headless Chromium, zero dialogs/pageerrors/executed code). First build to directly target "A book on Stress and Coping" and "Public education initiatives around empathy and AI," both named PROFILE.md projects with zero prior builds; reuses the taxonomy × compatibility-rules × novelty-scoring architecture proven by the 2026-07-12 Research Question Forge build, applied to a genuinely different topic to sidestep the "one Claude prompt replicates this" critique that scored 2026-06-24's AI Lecture Builder a 2/10 | Python 3, stdlib only, Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-07-22 | E | ambitious | Bayes Lab | Browser trainer for Bayesian inference on proportions (the Beta-Binomial conjugate model) — 5 psychology/neuroscience-grounded scenarios (clinical response rate, screening-tool positive rate, manipulation-check pass rate, replication success rate, custom), two prior-elicitation modes (belief-based "target rate + equivalent prior sample size" and direct α/β), live sequential/batch trial entry with Undo/Reset driving a hand-drawn Canvas 2D prior-vs-posterior overlay chart and a running history table, a Bayesian summary panel (mean/mode/SD/95% credible interval/P(θ>p0)), a Savage-Dickey Bayes factor panel with a Jeffreys/Lee & Wagenmakers strength label, and a side-by-side frequentist contrast panel (Wilson score CI, exact two-sided binomial test) explicitly labeled to teach the credible-vs-confidence-interval interpretation difference; optional Claude Haiku plain-English narrative via a session-only browser-supplied API key, always falling back to a deterministic template. All core statistics (regularized incomplete beta via continued fraction, Wilson interval, exact binomial test) were derived and cross-checked against an independent stdlib-only Python reference implementation before the JS was written. First build in the catalog to address "Develop advanced Bayesian statistical workflows," a learning goal named verbatim in PROFILE.md and untouched by the two existing frequentist-only Learning Aid builds (Power Lab: power analysis; Stats Coach, 2026-06-25, unmerged: test selection) | Vanilla HTML/CSS/JS (classic scripts, no ES modules so it opens directly via file://), native Canvas 2D, Playwright | complete | — | — |
| 2026-07-24 | G | ambitious | Heuristic Hunt | Browser vignette game training recognition of 12 cognitive biases and decision-making heuristics (anchoring, confirmation bias, sunk cost, loss aversion, overconfidence, hindsight, framing, base rate neglect, bandwagon effect, recency bias, survivorship bias, availability heuristic) across 30 hand-authored scenarios spanning research/lab, investing, and everyday-life domains — 3 chapters of increasing subtlety gated by a 70%-accuracy unlock, a date-seeded 5-question Daily Challenge (deterministic per UTC date, one play per day, shareable emoji-grid result), a Practice-by-bias drill mode, and a persistent per-bias Mastery Dashboard color-coded by accuracy. Fresh idea generated after the two category-G backlog ideas lost the lottery draw (roll 89 > 25% chance); ties directly to PROFILE.md's named "Human motivation and decision making" interest and quantitative-investing hobby, and reuses Confound Hunter's (2026-07-15) proven chaptered-vignette-game architecture applied to a new taxonomy | Vanilla HTML/CSS/JS (classic scripts, no ES modules so it opens directly via file://), Playwright, @playwright/test 1.56.1 | complete | — | — |
| 2026-07-25 | H | ambitious | BugTrace: Personal Bug-Pattern Miner | Python CLI that mines a developer's own bug-fix commit history — one or more local repos (no credentials needed) or every GitHub repo they own via GITHUB_TOKEN — and classifies each fix into one of 12 root-cause categories (null handling, off-by-one, race condition, type mismatch, missing error handling, config/env, dependency, logic/operator, API misuse, typo, test-only, other) using a deterministic keyword rule engine, with an optional Claude Haiku second-opinion pass that falls back to the keyword result on any network/parse failure; every diff excerpt is redacted for secret-like patterns before storage or any AI call; results persist in local SQLite (deduplicated by repo+sha, so a commit is never re-classified) and render as a terminal summary, JSON export, or a self-contained dark-mode HTML dashboard (Chart.js 4.4.4 category/trend charts with a verified graceful text-table fallback when the CDN is blocked, searchable fix list, AI-or-template coaching paragraph). Fresh idea generated after the one pending Category H backlog idea (a GitHub Actions analyzer, now redundant with the already-built ci-pulse) lost the 25%-threshold lottery draw (rolled 73); targets PROFILE.md's "Become substantially stronger as a Python developer" learning goal with an evidence-based self-improvement feedback loop no prior build or standard linter provides. Manually verified end-to-end against this repo's own real 24-month commit history (15 real fix commits correctly detected, classified, and deduped on re-sync) and live in headless Chromium (zero page errors, CDN-blocked fallback and search/filter both exercised) | Python 3, stdlib only, GITHUB_TOKEN (optional), Anthropic API (optional, via urllib), SQLite, Chart.js 4.4.4, pytest | complete | — | — |
| 2026-07-26 | I | ambitious | TripKit: Weather-Aware Trip Prep & Packing Planner | Python CLI that resolves a typed destination via the free Open-Meteo geocoding API, then fetches either a live daily forecast (trips starting within 16 days) or a climate-normal estimate averaged from the same calendar dates across the last 5 years of Open-Meteo's historical archive (trips further out, clearly labeled as an estimate, not a forecast, with per-year fetch-failure resilience), and feeds that into a deterministic packing rule engine keyed on temperature band/precipitation/wind/trip duration/activity tags (conference, cottage, boating, golf, outdoor, business, leisure)/destination country, producing a categorized (Clothing, Gear, Documents & Admin, Health & Comfort) checklist; local SQLite persistence; an optional Claude Haiku trip-briefing paragraph with an unconditional deterministic-template fallback (no network call attempted without a key); and a self-contained dark-mode HTML dashboard (Chart.js 4.4.4 temperature/precip chart, forecast-vs-historical badge, all user text `html.escape`d — verified live in headless Chromium against two separate script-injection payloads with zero page errors — checklist check-state persisted via localStorage and confirmed to survive a reload). First build to directly target PROFILE.md's named "travel," "cottage life," "boating," and "golf" hobbies, none of which had a dedicated build; fresh idea generated after the Category I backlog held zero pending rows, chosen over a cottage/boat maintenance scheduler and a cross-domain habit tracker (see WhyThis.md) | Python 3, stdlib only, Open-Meteo Geocoding/Forecast/Archive APIs, Anthropic API (optional, via urllib), SQLite, Chart.js 4.4.4, pytest | complete | — | — |
| 2026-07-27 | A | ambitious | SiliconWatch | Python CLI/dashboard comparing 12 curated AI-infrastructure and semiconductor companies (GPU/Accelerators, Custom Silicon/Networking, Foundry/IDM, Equipment/EDA, Memory, IP/Architecture & Analog) via live `yfinance` data — price, market cap, trailing/forward P/E, PEG, profit margin, revenue growth, analyst target, and a year of daily closes — persisted in local SQLite (deduplicated by ticker+day so repeated syncs build a real multi-week valuation/margin trend rather than overwriting); renders a self-contained dark-mode HTML dashboard (Chart.js 4.4.4 market-cap/margin/price/sector-P/E-over-time charts with a verified graceful text-table fallback when the CDN is blocked, sortable/searchable/filterable company table, per-ticker price-history dropdown) plus an optional Claude Haiku sector narrative (aggregated public numbers only) with an always-available deterministic-template fallback. Category A lottery drew a verbatim duplicate of the already-built 2026-06-21 GitHub Repository Health Scorecard; overridden to fresh generation (documented in WhyThis.md) rather than rebuild a zero-differentiation duplicate — first build to target PROFILE.md's named "AI infrastructure and semiconductors" rabbit-hole topic. Manually verified end-to-end in headless Chromium: same-day double-sync confirmed non-duplicating (12 rows, not 24), Chart.js CDN genuinely blocked and fallback engaged with zero page errors, dropdown/search/sort interactions all exercised live | Python 3, yfinance, SQLite, Anthropic API (optional, via urllib), Chart.js 4.4.4, pytest | complete | — | — |
| 2026-07-28 | B | ambitious | Voiceprint | Python CLI that audits a Markdown/text writing draft (blog post, book chapter, manuscript prose) for the lexical and structural patterns that make prose read as AI-generated or formulaic — a curated ~65-phrase AI-tell detector with line numbers, em-dash/semicolon/hedge-word density, a passive-voice heuristic, rule-of-three triad detection, sentence-length burstiness (coefficient of variation), vocabulary diversity (type-token ratio), and repeated-paragraph-opener detection — aggregated into an explainable, weighted 0–100 Human Voice Score (every weight a named constant, no black-box model); local SQLite history keyed by file path so re-running on later revisions of the same draft tracks score trend over time; an optional Claude Haiku holistic second opinion on the 3 worst-scoring paragraphs, with an unconditional deterministic-template fallback (zero network calls without a key); terminal, JSON, and self-contained dark-mode HTML report output. Both pending Category B backlog ideas turned out to be undocumented duplicates of already-completed builds (Worklog, Morning Briefing) — corrected in `builds/ideas.md` and built fresh instead. First build to target PROFILE.md's named "Blog writing and editing" friction point and the explicitly-remembered preference "I dislike writing that sounds obviously AI-generated." Manually verified end-to-end: a hand-written flagged draft scored 44.0/100 (17 AI-tell phrases named with line numbers, passive voice and rule-of-three both flagged), a cleaned revision of the same file scored 96.2/100, and `history` correctly showed both runs with a +52.2 delta; HTML report rendered live in headless Chromium with zero page errors, Chart.js CDN genuinely blocked and the fallback (hidden canvas, plain history table) engaged correctly | Python 3, stdlib only, Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-07-29 | C | ambitious | Citation Vault | Python CLI + self-contained dark-mode HTML dashboard: a personal research-reading and citation ledger, distinct from this catalog's two topic-scoped paper *discovery* feeds (Paper Lens, PubMed Research Radar). Adds papers by DOI (free, no-auth Crossref API) or free-text search or manual entry, tracks each through a to-read → reading → read → cited workflow with timestamped personal notes and tags, a tag-overlap + recency "resurface" nudge that surfaces settled reading newly relevant to the current to-read queue, and one-command BibTeX export filterable by tag/status. Optional Claude Haiku concept-tag suggestion and resurface rationale, both with an unconditional deterministic fallback (zero network calls without `ANTHROPIC_API_KEY`). Category C's backlog held zero pending rows, so this was freshly generated; targets PROFILE.md's named "Literature reviews" friction point and the "write grants and manuscripts" workflow, which today requires manually re-finding and re-formatting references. Manually verified end-to-end (DOI/manual add, all 4 status transitions, notes, tags, `--ai-tag` with no key confirming the deterministic fallback and zero Anthropic calls, `resurface`, `export bibtex`, `render`) and live in headless Chromium: zero page errors, zero dialogs, and an injected `<script>alert(1)</script>` payload in a paper title rendered as inert text, not executed | Python 3, stdlib only, Crossref API, Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-07-30 | D | ambitious | Vizstract | Browser tool that turns structured study metadata into a downloadable visual-abstract graphic — a deterministic SVG layout engine (5 study-design templates: Comparison of Groups, Correlational/Relationship, Process/Timeline/Intervention, Survey/Cross-Sectional, Before-After/Pre-Post) built from a generic region interpreter (iconBox/connector/iconBadge) over a ~22-icon hand-authored inline SVG library, a canvas-measurement-based text-fitting algorithm (wrap → shrink-font → ellipsis-truncate, verified to never overflow its box), and 5 accessible light-background color themes; exports as standalone SVG and PNG (client-side Canvas rasterization, no server); a localStorage library with save/load/delete/"New entry" actions so one session can independently build multiple abstracts; optional Claude Haiku field extraction from a pasted free-text abstract (direct browser call, session-only key, never persisted) with an always-available deterministic regex/keyword fallback making zero network calls when no key is supplied. Category D's backlog held zero pending rows, so this was freshly generated; targets PROFILE.md's named "write grants and manuscripts" and "public-facing articles" friction points with a genuinely new deliverable — a downloadable graphic file — that no prior build produces; explicitly designed to avoid AI Lecture Builder's (2026-06-24, 2/10, "a power user replicates this with one prompt") failure mode by making the core value a deterministic rendering/layout engine rather than a prose wrapper, and to avoid a third build on Research Question Forge/Bridgework's taxonomy-cross-product architecture. Manually verified end-to-end in headless Chromium: all 5 templates screenshotted, and two coordinate-overlap bugs (a direction-icon badge overlapping an adjacent box in the `process` and `survey` templates) found and fixed during that QA; a `<script>` payload in a live-typed field and a persisted `<img onerror>` payload in a reloaded library entry both verified inert (zero dialogs, zero executed code); the deterministic-fallback path confirmed to make zero network requests with no API key, and the AI path confirmed to make exactly one POST against a mocked Anthropic endpoint | Vanilla HTML/CSS/JS (classic scripts), native SVG + Canvas 2D, Anthropic API (optional, direct browser call), Playwright | complete | — | — |
| 2026-07-31 | E | ambitious | Signal Detection Lab | Interactive browser trainer for Signal Detection Theory (d′, criterion, ROC/AUC) grounded in forensic and affective-neuroscience research paradigms — an Explainer tab with a Canvas 2D dual-Gaussian plot and a mouse-draggable criterion line live-updating d′/c/β/hit-rate/FA-rate; an ROC Explorer tracing the curve for the current d′ against a closed-form AUC = Φ(d′/√2), cross-checked against trapezoidal numeric integration; a Calculator accepting raw hit/miss/false-alarm/correct-rejection counts with a togglable Hautus (1995) loglinear correction for 0%/100% edge rates, reporting d′/c/β plus the nonparametric A′/B″ measures; and a Scenario Quiz with 6 hand-authored cases (recognition memory in psychopathy research, fearful-face threat detection, eyewitness lineup identification, diagnostic screening, deception judgment, radiological detection) whose correct answers are always derived live from the same math, never hardcoded, with localStorage-persisted scoring. Optional Claude Haiku practice-scenario generation from a typed research context, with a deterministic string-seeded fallback making zero network calls when no key is supplied. All core math (erf/normalCdf, Acklam's normalQuantile, ROC/AUC) implemented from scratch and cross-checked (percentile round-trips, closed-form-vs-numeric AUC agreement, bias-symmetry properties) before UI wiring. Day-of-year rotation (212 → Category E); the Category E backlog held zero pending rows, so this was freshly generated — fills a real gap left by the three prior Learning Aid builds (Power Lab: statistical power, CircuitLab: neuroanatomy, Bayes Lab: Bayesian inference), none of which cover SDT despite it underpinning several of PROFILE.md's named forensic/affective-neuroscience research paradigms. Manually verified end-to-end in headless Chromium (all 4 tabs screenshotted, hand-cross-checked stat values matched the on-screen numbers); a `<script>` payload typed into the AI-context field and a persisted `<img onerror>` payload from a mocked AI response both verified inert (zero dialogs, zero executed code); the deterministic-fallback path confirmed to make zero network requests with no API key, and the AI path confirmed to use exactly one mocked Anthropic response | Vanilla HTML/CSS/JS (classic scripts, no ES modules so it opens directly via file://), native Canvas 2D, Anthropic API (optional, direct browser call), Playwright | complete | — | — |
| 2026-08-01 | F | ambitious | ItemScope | Python CLI that turns a CSV of student exam/quiz responses (binary right/wrong, or raw MCQ option letters plus an answer key) into a classical-test-theory item analysis report — per-item difficulty (p-value), point-biserial discrimination against the corrected total score, KR-20 test reliability, and upper/lower-27%-group distractor analysis flagging non-functioning and reversed-pull distractors, with all math implemented from scratch and cross-checked against Python's stdlib `statistics.correlation` and a hand-worked KR-20 reference; zero-variance items and single-item inputs report a labeled "undefined"/"not meaningful" result instead of crashing or returning NaN. Renders a self-contained dark-mode HTML dashboard (native Canvas 2D difficulty-vs-discrimination quadrant scatter, sortable/searchable item table, flagged-items panel) plus terminal/JSON output; optional Claude Haiku plain-English revision suggestions for the 3 worst-flagged items (aggregated stats only, never raw responses), with an unconditional deterministic-template fallback. Category F's 2-idea backlog lottery (27% chance) missed on a roll of 54, so this was freshly generated to extend the category's strongest pattern to date — the 2026-06-17 Qualtrics Survey Data Inspector remains the catalog's highest-rated build at 9/10, and this build applies the same "verifiable-statistics QC report" shape to PROFILE.md's named "Student evaluation workflows" friction point, a friction point no prior build had touched. Manually verified end-to-end: hand-designed 24-student sample datasets (binary and raw-option) produced exactly the expected p-values/discrimination signs/flags; live in headless Chromium (via the system Node Playwright install) the report's sort/search/quadrant-chart all worked with zero page errors, and both a `<script>` and an `<img onerror>` payload placed in an item ID were confirmed inert (zero dialogs, zero executed code) | Python 3, stdlib only, Anthropic API (optional, via urllib), pytest | complete | — | — |
| 2026-08-02 | G | ambitious | Lexicon | Browser letter-guessing word puzzle (Wordle-style green/yellow/gray feedback) built entirely from real technical vocabulary across four of the user's own domains — neuroscience/psychology, statistics/research methods, AI/machine learning, and investing — a 48-word hand-curated bank (12 per category, each with an accurate one-sentence clue) with a deterministic seeded-shuffle daily cycle (same word for everyone on a given UTC date, no repeats until the full 48-day cycle completes), one-play-per-day gating plus an unlimited per-category Practice mode, a two-pass duplicate-letter-correct guess evaluator, a definition-clue hint auto-revealed after 2 wrong guesses, an optional AI bonus hint via a direct session-only-key browser call to the Anthropic API (unconditional deterministic fallback, zero network calls with no key), a shareable emoji-grid result, a colorblind-mode shape-marker toggle, and a persistent localStorage stats dashboard (streaks, win %, per-category accuracy). First build in the catalog to use a letter-guessing mechanic — the four prior Category G builds cover regex-writing, trivia quiz, category-sorting, and vignette-quiz shapes, and two of those (Confound Hunter, Heuristic Hunt) already share an identical chaptered-vignette shape, so a fifth build reusing either existing shape was passed over for a genuinely new one. Category G's 2-idea backlog (both investing-only guessing games) lost the 25%-chance lottery draw on a roll of 32, which also steered the choice: investing had already appeared once in the last 10 builds (SiliconWatch) and a second investing-only game would have skewed topic weight while ignoring three other named profile domains a cross-domain word bank could cover in one build. A hand-verification pass caught and fixed a genuine date-arithmetic bug during the build (`Date.UTC`'s 0-indexed month parameter fed 1-indexed `YYYY-MM-DD` values directly, silently shifting every computed date by up to a month) via a dedicated `daysBetween` test before it could reach the daily-word scheduling logic | Vanilla HTML/CSS/JS (classic scripts, no ES modules so it opens directly via file://), Anthropic API (optional, direct browser call), Playwright | complete | — | — |
| 2026-08-03 | H | ambitious | Landing Pattern | Python CLI that fetches every open PR on a GitHub repo via `GITHUB_TOKEN`, classifies each one's merge readiness (ready, merge conflict, CI failing, changes requested, CI pending, awaiting review, behind base, draft, unknown), builds a changed-file overlap graph across the whole backlog, and produces a two-batch recommended merge order — Batch 1 (no file overlap with an earlier PR, safe to merge back-to-back) and Batch 2 (ready but will need a rebase after Batch 1 lands) — plus a Blocked list sorted by how actionable the blocker is. Renders terminal, JSON, or a self-contained dark-mode HTML report; optional Claude Haiku one-sentence notes on blocked PRs with an unconditional deterministic-template fallback; local SQLite persistence so repeat `sync` runs build real per-PR readiness history via a `history` command. Read-only — never merges, closes, or comments. Orientation this session found this repo itself carries 50 open PRs going back to 2026-06-11, none merged since 2026-06-18, so the build was validated against a real 10-PR sample of this exact backlog rather than only synthetic fixtures: `mergeable_state` was `clean` for all 5 newest PRs and `dirty` for all 5 oldest sampled, and all 5 clean PRs turned out to share changed files (`builds/index.md`, `builds/ideas.md`), which the tool correctly split into one Batch-1 PR and four Batch-2 demotions. Category H's 3-idea backlog (all previously passed-over or a stale duplicate of the already-built `ci-pulse`) lost the 25%-chance lottery draw (rolled 100), so this was freshly generated — extends Pipeline Pulse's (2026-07-09) backlog-detection with the sequencing/conflict-risk question it deliberately left unanswered | Python 3, stdlib only, GITHUB_TOKEN, Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-08-04 | I | ambitious | Dockside | Python CLI that tracks recurring cottage/boat seasonal maintenance tasks (dock installation/removal, winterizing, engine service) and scores each one's real-world readiness against live Open-Meteo Forecast + Marine API data — a deterministic multi-constraint engine (max wind, minimum consecutive dry days, minimum water temperature via hourly sea-surface-temperature resampling, frost-free requirement) evaluates every day in the current 7-day window per task and classifies it as ready_now/ready_soon/not_ready/overdue/off_season/done_this_season, with a per-constraint pass/fail/unknown breakdown (unknown when a site has no marine model coverage — many inland lakes don't). A separate deterministic "boating comfort score" (0-100, wind/wave/precip/temp composite) gives a general week-ahead outlook independent of any task. `complete` marks a task done and auto-schedules next season (leap-year-safe). Renders a self-contained dark-mode HTML dashboard (Chart.js 4.4.4, DOM-built text-table fallback verified live in headless Chromium when the CDN was genuinely blocked by this container's proxy) plus an optional Claude Haiku season briefing with an unconditional deterministic-template fallback (verified to make zero network calls with no API key, both in pytest and manually). Category I's lottery drew a documented-weak backlog idea (Momentum: GitHub-signal habit tracker, already critiqued in July as redundant with 7+ existing GitHub-backed builds and reliant on manual logging) — overridden to fresh generation per the SiliconWatch precedent, full reasoning in WhyThis.md. First build to use the Open-Meteo Marine API and the first Category I build to target the named "boating"/"cottage life" hobbies with a recurring season-long lens rather than TripKit's (2026-07-26) one-off trip-packing lens. Manually verified end-to-end in headless Chromium with two different script-injection payloads in task names (zero dialogs, zero injected DOM nodes, escaped text only) — a live `innerHTML`-from-string-concat issue in the chart CDN-blocked fallback path was found and fixed (rewritten to safe `createElement`/`textContent` DOM construction) during that verification pass | Python 3, stdlib only, Open-Meteo Geocoding/Forecast/Marine APIs, Anthropic API (optional, via urllib), SQLite, Chart.js 4.4.4, pytest | complete | — | — |
| 2026-08-05 | A | ambitious | Impact Ledger | Python CLI that turns the free, no-auth OpenAlex API into a personal research-impact dashboard — `search-author` disambiguates a name into a specific OpenAlex author ID (never hardcoded), `sync` fetches the author's full work list plus citation counts and reconstructs each paper's abstract from OpenAlex's word-position inverted index, and persists a dated snapshot in local SQLite (same-UTC-day re-syncs upsert rather than duplicate, so a genuine multi-run citation history accumulates exactly like this catalog's other snapshot-based builds). A deterministic velocity query flags "rising papers" — citation count increased since the immediately preceding distinct snapshot — with an optional one-sentence Claude Haiku note on why a paper might be gaining traction (unconditional deterministic-template fallback, zero network calls with no `ANTHROPIC_API_KEY`, verified in pytest). Renders a self-contained dark-mode HTML dashboard (hero stats, Chart.js 4.4.4 citation-growth line chart with a DOM-table fallback, sortable/searchable paper table, Rising Papers panel) with all dynamic data passed to the browser as an escaped JSON blob and every node built via `createElement`/`textContent`. Day-of-year rotation (day 217 → Category A); the 3-idea Category A backlog's 27%-chance lottery missed (rolled 100), so this was freshly generated — the first Category A build oriented around the user's own scholarly output rather than GitHub metrics or markets/macro data, targeting PROFILE.md's named "Grant writing" and "write grants and manuscripts" friction points. Manually verified end-to-end in headless Chromium: a `</script><script>` payload in a paper title and an `<img onerror>` payload in an AI note both rendered as inert escaped text (zero dialogs, zero page errors, zero injected globals); with the Chart.js CDN genuinely blocked, the citation-trend fallback table, live search filter, and column-sort all worked correctly against real snapshot data | Python 3, stdlib only, OpenAlex API, Anthropic API (optional, via urllib), SQLite, Chart.js 4.4.4, pytest | complete | — | — |
| 2026-08-06 | B | ambitious | Manuscript Pipeline | Python CLI that tracks a researcher's own manuscripts through the submission → under-review → revise-and-resubmit → accepted/rejected → published pipeline in local SQLite (append-only status history, never overwritten). `capture` extracts journal/decision/deadline from a pasted decision email via a deterministic regex/keyword parser, with an optional Claude Haiku enrichment pass (unconditional fallback, zero network calls without `ANTHROPIC_API_KEY`, verified in pytest). `sync` queries the free, no-auth Crossref API by title + first-author surname and auto-transitions a manuscript to `published` (recording DOI and publication date) only when title token-overlap similarity is ≥0.72 **and** an author surname matches — the catalog's first build to auto-detect a real-world status change via a live API rather than relying solely on manual updates. Renders a terminal summary and a self-contained dark-mode HTML dashboard (funnel chart with a DOM-table CDN-blocked fallback, sortable/searchable manuscript table, at-risk panel) with the manuscript data JSON-escaped for safe `<script>`-tag embedding (a real bug — using `html.escape()` there would have corrupted the JSON once read via `.textContent`, since `<script>` content is HTML raw text and never HTML-entity-decoded — was found and fixed during the test pass, along with a keyword-substring false-positive that mis-classified "unable to accept" emails as acceptances, and a stdin-read bug on an empty `--text ""` flag). Category B's own backlog lottery pool turned out to be two stale `pending` rows both already built and never marked `built` (`Worklog`, 2026-07-10; `Morning Briefing`, 2026-06-22) — corrected in `builds/ideas.md` before the (now-empty) pool skipped straight to fresh generation. First build to target manuscripts still in the submission pipeline, distinct from Citation Vault (already-read literature), Paper Lens/PubMed Research Radar (literature discovery), and Impact Ledger (citation impact of already-published work) | Python 3, stdlib only, Crossref API, Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-08-07 | C | ambitious | Waymark | Python CLI that automatically mines git commit history across the user's own local repositories — via real `git log`, never mocked, since git is a local tool rather than an external network API — into a searchable, cross-project SQLite knowledge base of *decisions*, with zero manual note-taking required. A deterministic, fully-unit-tested heuristic scorer (0–10) rates every commit's decision-worthiness from conventional-commit type, breaking-change markers, size, and decision-language keywords ("because", "revert", "workaround", "trade-off"); a deterministic fallback summary is always present. `index` incrementally walks a repo (re-indexing skips already-seen hashes by commit hash), `search` ranks/filters by repo/tag/date/min-score, `list-repos` shows per-repo indexed/decision-worthy counts, and `render` builds a single self-contained dark-mode HTML dashboard (live client-side search/filter, click-to-expand detail, embedded JSON payload verified safe against a script-injection payload in a commit subject). Optional `enrich` command calls Claude Haiku to rewrite high-scoring commits' summaries into clearer plain-English sentences (only commit subject/body/file-list/stat-counts sent, never full diff content; unconditional deterministic fallback verified to make zero network calls without `ANTHROPIC_API_KEY`). Category C's backlog held zero pending rows, so this was freshly generated — directly answers a specific piece of prior rating feedback rather than just avoiding a topic that scored poorly: the 2026-06-06 AI Session Context Bridge build (3/10) was marked down because it "requires manual entry... would score higher with auto-capture of git state," and nobody had acted on that note until tonight. Manually smoke-tested end-to-end against this repository's own 56-commit history (real data, not just synthetic fixtures): `index` correctly scored all 56 commits (31 decision-worthy), `search --min-score 5` returned a sensibly-ranked list, `render` produced a working dashboard | Python 3, stdlib only, Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-08-08 | D | ambitious | Panel Prep | Python CLI that turns a pasted/loaded grant-proposal draft (Specific Aims/Significance/Innovation/Approach/Rigor & Reproducibility) into a mock NIH study-section critique — a deterministic, regex-based completeness/rigor checklist (numbered aims with hypothesis language, an explicit significance gap, an innovation claim, sample-size/power justification, timeline, pitfalls/alternatives, preliminary data, statistical plan, sex-as-biological-variable/blinding/randomization/authentication/reproducibility language) scored by three fixed reviewer personas (Rigor Hawk, Vision Advocate, Generalist), each with a distinct section-weighting, producing an NIH-style 1(best)–9(poor) score with rationale drawn from the actually-failed checks — fully functional and independently useful with zero API key. An optional Claude Haiku call per persona (only Significance/Innovation/Approach/overall-Impact are ever scored — Investigator/Environment are deliberately never guessed, since they require CV/facilities data this tool never has) replaces the deterministic critique with richer narrative review in that persona's voice, unconditionally falling back to the deterministic result on any missing key, network error, or malformed response. Local SQLite persists every `submit` as a new, permanent version per named project (never overwritten), so `history` and the self-contained dark-mode HTML report (checklist matrix, persona cards, resume-of-discussion synthesis, Chart.js 4.4.4 trend line with a DOM-table CDN-blocked fallback) show real score-over-revisions trend. Day-of-year rotation (day 220 → Category D); the backlog held zero pending Category D rows, so this was freshly generated to directly answer AI Lecture Builder's (2026-06-24, 2/10, "a power user replicates this with one prompt") failure mode by making the deterministic checklist/scoring engine — not AI prose — the core value, and to target PROFILE.md's verbatim-named "Grant writing" friction point, which no prior build (Protocol Forge: ethics compliance; Manuscript Pipeline/Impact Ledger: post-submission tracking; Citation Vault: reading ledger) had addressed as pre-submission content critique. Manually verified end-to-end: the complete sample proposal scored a perfect 1.0/100% on every persona and check, a deliberately sparse revision to the same project scored 9.0/0% (confirming genuinely monotonic scoring on real input, not just synthetic unit tests), `history`/`list`/`render` all ran correctly against the resulting two-version project, a live `</script><script>alert('xss')</script>` payload placed directly in proposal text was confirmed present and correct inside the report's embedded JSON but never appears as an unescaped, executable tag anywhere in the surrounding HTML, and a mocked-`urlopen` run confirmed exactly zero network calls through the real CLI path with no `ANTHROPIC_API_KEY` set | Python 3, stdlib only, Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-08-09 | E | ambitious | Portfolio Lab | Interactive Modern Portfolio Theory trainer — diversification, the efficient frontier, and the Sharpe ratio, all computed from real historical prices for a fixed 12-asset teaching basket (8 equity sectors plus a gold ETF and a long-term Treasury ETF chosen for low/negative-correlation contrast) fetched locally via `yfinance` (`fetch_data.py`, run by the user — the build container's network policy blocks live Yahoo Finance access, as expected and irrelevant to the design). All math implemented from scratch in `src/math.js` — Gauss-Jordan matrix inversion, the closed-form (unconstrained) Markowitz two-fund efficient frontier, and the closed-form tangency/max-Sharpe portfolio (Merton 1972) — verified against 15 provable correctness properties (global-minimum-variance dominance over 500 sampled portfolios, frontier dominance over the Monte Carlo cloud at matching return levels, tangency as a true local Sharpe maximum) rather than approximate visual checks alone. Five tabs: a two-asset mixer with a live risk-return "bullet curve," the full 12-asset efficient frontier plotted against a resample-able long-only Monte Carlo cloud, a risk-free-rate slider driving the tangency portfolio and Capital Market Line, a clickable 12×12 correlation heatmap, and a quiz mode whose correct answers are always derived live from the real covariance data. Ships honest by default — the committed `data.js` is `PORTFOLIO_DATA = null` until the user runs `fetch_data.py`; no fabricated market numbers ever shown. Optional direct-browser Claude Haiku plain-English explanations (session-only key, never persisted) with an unconditional deterministic-template fallback built from the same real numbers. First Learning Aid build to use real external market data — the four prior Category E builds (Power Lab, CircuitLab, Bayes Lab, Signal Detection Lab) were all synthetic client-side math with no live data source — and the first build in the catalog to target PROFILE.md's verbatim "Continue learning quantitative investing and algorithmic trading" learning goal. Category E's backlog held zero pending rows, so this was freshly generated over two other untouched-topic candidates (a Git-internals visualizer, an agent-orchestration sandbox — both recorded in `builds/ideas.md`), chosen because it was the only one tied to a real, free, named PROFILE.md data source rather than being purely synthetic. A manual QA pass against a realistic mocked 12-ticker dataset caught two real bugs before commit: a missing `id` attribute on the data-summary div that silently halted all UI/tab/quiz wiring (`init()` threw before reaching `showApp()`'s sibling setup calls), and a numerical Sharpe-ratio search bounded to a heuristic display range that put the tangency marker at the chart's edge regardless of the risk-free rate — replaced with the exact closed-form tangency formula, independent of any display range | Python 3, yfinance (runtime), pandas, numpy, Vanilla HTML/CSS/JS (classic scripts, no ES modules, opens directly via file://), native Canvas 2D, Anthropic API (optional, direct browser call), pytest, Playwright | complete | — | — |
| 2026-08-10 | F | ambitious | Ingest Gate | Browser CSV quality inspector for The Canada List's own ingestion pipeline — a from-scratch RFC4180-style parser (quoted/embedded-comma/newline fields, escaped quotes, BOM, CRLF) feeds a configurable schema engine (required/type/enum/unique-key columns, editable in-app, persisted to localStorage, exportable/importable as JSON, with a "load from this file's header" helper and a Canada-List-style default preset) that flags missing required columns/values, malformed (ragged) rows, invalid URL/email/number/date/enum values, whitespace, and encoding corruption (Unicode replacement-character detection plus a `TextDecoder`-based UTF-8/Windows-1252 strict-decode check), alongside a separate exact-row and normalized-unique-key (case/whitespace/URL-protocol-insensitive) duplicate detector. Renders a searchable/filterable/sortable issues dashboard (row-click detail view, zero `innerHTML` from file data — verified live in headless Chromium against an `<img onerror>` + `</script><script>` payload with zero dialogs and zero injected DOM) plus two CSV downloads (cleaned file with an appended `QC_Flags` column; a standalone issues report). Local-only Run History persists aggregate counts (never row content) across sessions. Optional Claude Haiku "Data Quality Briefing" is architecturally guaranteed privacy-safe — its only input is the aggregate summary object, never a parsed row or cell value — with an unconditional deterministic template fallback, matching the direct-browser-call convention established by CircuitLab/Bayes Lab/Signal Detection Lab. First build in the catalog to target The Canada List, a named active project with zero prior builds despite PROFILE.md explicitly naming "The Canada List ingestion and quality control pipeline" as a friction point. Selected via the category-F lottery (27% chance, rolled 9; weighted 5/12 draw against a second pending idea) rather than fresh generation. A manual visual-QA pass in headless Chromium (screenshots of all 3 tabs against a seeded 6-row fixture with a malformed row, missing value, bad URL, bad enum, and a whitespace+duplicate row) caught and fixed a real bug the 44 automated tests missed: the validator's message-templating helper conflated a generic extra-argument slot with its (column, value) pairing, so `invalid_url`/`invalid_enum`/etc. messages rendered `"undefined" in column "X"` instead of the real offending value, and `malformed_row` rendered `expected null` instead of the true header length | Vanilla HTML/CSS/JS (classic scripts, no ES modules, opens directly via file://), Anthropic API (optional, direct browser call), Playwright | complete | — | — |
| 2026-08-11 | G | ambitious | Quarter Call | Browser chart-reading game: shows a real historical stock price chart (6 months up to a fixed decision date) and asks the player to call whether the price went Up, Down, or Flat (±5%) over the following quarter, using genuine Yahoo Finance daily closes for a curated 48-round bank across 11 sectors and 2016–2023 (every decision date years-settled, never a live/predictive claim). Ships honest by default — `data/rounds-data.js` commits as `ROUNDS_DATA = null` until the user runs `fetch_data.py` locally (this build container's egress proxy returns 403 on `query1.finance.yahoo.com`, confirmed live this session), following the same pattern the 2026-08-09 Portfolio Lab build established. Practice mode (shuffled, no-repeat-until-exhausted draws) and a UTC-date-seeded Daily Challenge (5 rounds, one playthrough/day, shareable 🟩/🟥 emoji result) both draw from the same bank; sector/industry badges plus two metrics (trailing 6-month return, annualized volatility) are computed directly from the same closes shown in the chart, so they can never disagree with what's on screen. Native Canvas 2D chart rendering (no library), persistent localStorage stats (streak/accuracy/per-sector breakdown), and an optional direct-browser Claude Haiku historical-context note on reveal (aggregate-only prompt, unconditional deterministic fallback). Selected via the category-G lottery (25% chance, rolled 22; weighted 5/10 draw against the backlog's other pending idea, Market Cap Higher or Lower) rather than fresh generation — the first Category G build to use real external market data rather than entirely hand-authored content. A first Playwright run caught two real bugs the design itself didn't anticipate: an author-stylesheet `display` rule on `main`/`#reveal-panel` was silently overriding the `[hidden]` attribute's UA-stylesheet `display: none` (author CSS always wins over UA CSS regardless of specificity), and the API-key input originally lived inside the reveal panel — hidden until after the first guess, but the AI note fetches immediately on reveal, so there was no way to set a key before your first guess; moved the input to a permanently visible spot. Both fixed and re-verified; live headless-Chromium QA screenshots (mobile + desktop, all 5 primary states) showed zero page errors | Vanilla HTML/CSS/JS (classic scripts, no ES modules, opens directly via file://), native Canvas 2D, Python 3 (`fetch_data.py`, run by the user), yfinance 1.5.2, Anthropic API (optional, direct browser call), pytest, Playwright | complete | — | — |
| 2026-08-12 | H | ambitious | Snipvault | Personal code-snippet library: a local SQLite-backed store (title/language/code/description/tags/source/usage_count) with a full CLI (`add`/`search`/`get`/`list`/`remove`/`render`), a deterministic ranked keyword search (title > tags > description > code weighting, usage-count and recency tie-breakers), and an optional Claude Haiku layer — auto-generated description/tags on save (`--ai-enrich`) and natural-language query expansion into keyword terms before the same deterministic ranker runs (`search --ai`, deliberately never letting the LLM directly rank results) — with an unconditional deterministic fallback verified via real `urlopen` call-count assertions to make zero network calls without `ANTHROPIC_API_KEY`. Renders a self-contained dark-mode HTML dashboard (search/filter, click-to-expand code, copy button, all snippet data delivered via an escaped JSON payload read with `textContent`/`createElement`, DOM cleared via `removeChild` rather than `innerHTML`). Ships a companion Claude Code Skill (`skill/SKILL.md`) as the primary intended interface — copy into `.claude/skills/` so a session can save/find snippets on request ("save this as a snippet") — directly acting on Step 2f's guidance that a Skill is usually a better fit than a standalone script for developer tools, which no prior Category H build had taken literally. Category H's one pending backlog idea (#9, GitHub Actions Performance Analyzer) turned out to be a verbatim duplicate of the already-built `ci-pulse` (2026-06-28) that had slipped through one prior lottery draw undetected — corrected to `skipped` in `builds/ideas.md`, which emptied the pool and moved straight to fresh generation. Deliberately breaks from the pattern of 7 of the prior 9 Category H builds leaning on `GITHUB_TOKEN` — this one needs no GitHub API at all. Manually verified end-to-end (real SQLite round-trip across all six commands) and live in headless Chromium: two live injection payloads (`</script><script>alert(1)</script>` as a title, `<img onerror>` as code) rendered as inert text with zero dialogs and zero page errors, and the live search filter correctly narrowed results | Python 3, stdlib only, Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-08-13 | I | ambitious | Macro Kitchen | Python CLI that computes real calorie/macro targets from a Mifflin-St Jeor body-stats profile — optionally adjusted by a real Garmin Connect Activities CSV export's most recent 7-day training load (half the logged burn, capped, since Garmin's figure already includes resting metabolism TDEE covers) — and deterministically plans a 7-day meal week from a curated 54-recipe bank, scaling each chosen recipe's portion size (0.75x-2.0x, added mid-build after the tolerance test caught single-serving recipes structurally unable to reach higher-calorie targets) to close in on the day's calorie/protein budget within a verified ±10%. `grocery` aggregates all 28 meals' ingredients into a shopping list (scaled by the same portion multipliers); `render` produces a self-contained dark-mode HTML dashboard (hand-drawn Canvas 2D 7-day calorie line, per-day macro cards, grocery list, all data JSON-escaped for safe `<script>`-tag embedding). Optional Claude Haiku daily "chef's notes" send only aggregate macro numbers, never body stats, with an unconditional deterministic-template fallback. Category I's backlog held zero pending rows, so this was freshly generated — the four prior candidate domains (meal planner, budget tracker, habit log, checklist) left "meal planner" as the only one of CLAUDE.md's own named Category I examples with zero prior coverage across the category's six existing builds (weather-comfort planning, project context, spending, deadlines, trip packing, boat maintenance). First build to treat a Garmin Connect CSV export as a "real data" source. Manually verified end-to-end (profile → import-garmin → generate → list/show/grocery/render) and live in headless Chromium: zero page errors, zero dialogs, a `</script><script>alert(1)</script>` payload manually inserted into a plan's day-note via SQLite rendered as inert escaped text, not executable markup; confirmed the Garmin import measurably raised the calorie target (2615.6 → 2863.1 kcal) versus a `--no-garmin` run on the same profile | Python 3, stdlib only, Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-08-14 | A | ambitious | Earshot | Browser dashboard that measures real ambient sound level from the device microphone in real time via `getUserMedia` + Web Audio `AnalyserNode` (no mocked/simulated numbers — verified live under Chromium's `--use-fake-device-for-media-stream` flags, which supply a genuine synthetic `MediaStream` the app's own untouched audio pipeline processes end-to-end), converts it to an approximate dB(A) reading using a simplified A-weighting lookup table and a user-set calibration offset (clearly labeled "uncalibrated" until set), classifies it into NIOSH/WHO-style Quiet/Moderate/Loud/Hazardous zones, and logs venue-tagged sessions to `localStorage` with average/peak/duration and a NIOSH 3 dB-exchange-rate noise-exposure dose (cross-checked against 3 independent hand-computed reference cases). Self-contained dark-mode HTML dashboard (hand-drawn Canvas 2D live rolling chart, per-session detail chart, cross-session trend chart, sortable/searchable history table); optional direct-browser Claude Haiku plain-English exposure briefing sending only aggregate session numbers, never raw audio, with an unconditional deterministic-template fallback verified to make zero network calls with no key. First build in the catalog to target **Kwyeter**, a named PROFILE.md active project (environmental noise awareness / accessibility for tinnitus and sensory sensitivities) with zero prior coverage across all 63 previous builds. Category A's 2-idea backlog lottery (27% chance) missed on a roll of 84, and a known-stale duplicate idea (#5, verbatim match of the already-built 2026-06-21 GitHub Repository Health Scorecard, flagged once before on 2026-07-27 but never corrected) was corrected to `skipped` before the draw. Manually verified end-to-end live in headless Chromium under the same fake-device flags as the automated suite: zero page errors/console errors across all three tabs, a saved-session flow, and a 375px mobile viewport; a real UX issue was found and fixed during that pass — uncalibrated readings from the fake device's mostly-silent-with-periodic-bursts signal produced implausible-looking negative dB values with no cue that this was expected pre-calibration behavior, fixed by adding an explicit "(uncalibrated)" label rather than fudging the honestly-computed number; a `</script><script>` + `<img onerror>` payload placed in a venue name and note was confirmed to render as inert text with zero dialogs and zero injected DOM | Vanilla HTML/CSS/JS (classic scripts, no ES modules, opens directly via `file://`), native Web Audio API + Canvas 2D, Anthropic API (optional, direct browser call), Playwright | complete | — | — |
| 2026-08-15 | B | ambitious | Provenance | Python CLI that turns a CSV of business names into a Canadian-ownership research batch job — extends CanFile's (2026-07-20) proven single-lookup Wikidata rule engine (country/headquarters/parent-organization/owned-by claims, one-hop resolution, confidence tiers from direct P17 match down to owner-only inference, any conflicting signal forced to `uncertain` rather than guessing) into an actual batch processor: cache-first per business via a normalized-name-keyed local SQLite store (append-only versioning, never overwritten), so re-running a batch over an overlapping business list issues zero redundant Wikidata queries. CSV in, enriched CSV out (`verdict`/`confidence`/`evidence`/`wikidata_qid`/`ai_note` columns appended to whatever input columns exist); optional Claude Haiku one-sentence plain-English note on `uncertain` rows only, strictly gated so it can never override the deterministic verdict, with an unconditional fallback making zero network calls without `ANTHROPIC_API_KEY`; optional self-contained dark-mode HTML batch report (JSON-payload-in-`<script>`-tag delivery, `createElement`/`textContent`-only DOM construction, verified against a live XSS payload). Ships a companion Claude Code Skill (`skill/SKILL.md`), the first Category B build to do so, following the precedent Snipvault (2026-08-12) set. Both Category B backlog rows were pending-but-already-built duplicates (idea #4 → Worklog 2026-07-10; idea #7 → Morning Briefing 2026-06-22) — corrected before the lottery, which emptied the pool and routed to fresh generation; first build to treat The Canada List's "ingestion and quality control pipeline" friction point as a batch workload rather than CanFile's one-at-a-time lookup shape. Manually verified end-to-end (not just mocked): live `classify` run against this build container's actually-blocked Wikidata host degraded honestly to `uncertain`/confidence-0.0 rather than crashing (the documented, expected build-container network constraint); a second `classify` run over the same file confirmed real cache hits (`Cache hits: 3, Cache misses: 0`) and `history` correctly replayed the cached resolution from real SQLite I/O; the rendered HTML report round-tripped business data correctly with zero `innerHTML` | Python 3, stdlib only, Wikidata API, Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-08-16 | C | ambitious | Curriculum Atlas | Python CLI + self-contained dark-mode HTML dashboard that builds a local knowledge base from a professor's own syllabi/lecture text — three deterministic concept-extraction sources (explicit `[[marker]]` wiki-style tags, heading-topic extraction that deliberately excludes a document's own h1 title line, and a capitalized-phrase heuristic), deterministic objective-phrase extraction (with a soft-line-wrap-joining pass so a hand-wrapped objective sentence isn't truncated at the first newline), and three from-scratch analysis functions: cross-course concept overlap, Jaccard-token-overlap objective-gap flagging (hand-cross-checked against a worked reference example), and term-over-term concept diff. Optional Claude Haiku auto-marking (`ingest --ai-mark`) never invents a concept directly — it only inserts `[[...]]` markers into a copy of the source text, which is then re-parsed by the exact same deterministic parser used for hand-marked input; optional batched one-sentence concept notes (`--ai-notes`), both with an unconditional fallback making zero network calls without `ANTHROPIC_API_KEY`. First build in the catalog to target teaching/curriculum materials — PROFILE.md names three specific courses the user currently teaches plus "developing new AI-focused university courses" and "updating neuroscience curriculum," none previously built against across 66 prior builds. Category C's backlog held zero pending rows, so this was freshly generated; papers/citations already account for 3 of Category C's 7 prior builds (Paper Lens, PubMed Research Radar, Citation Vault), making a 4th paper-discovery tool the least novel option in the pool. Manually verified end-to-end against 3 hand-authored fixture syllabi with an intentionally shared concept and an intentionally uncovered objective (not just mocked): the live run caught and fixed two real bugs the 70 initially-passing unit tests had missed — a document's own title line (`# Course — Term`) was being mis-parsed as a heading-with-separator, producing a bogus "Fall 2026" concept falsely shared across all three courses, and a line-wrapped objective sentence was being truncated at the first newline. Both fixed, 4 new regression tests added (74 total), and re-verified live in headless Chromium: zero page errors, zero dialogs, zero injected `<script>`/`<img>` elements, and both an `<img onerror>` marker payload and a `</script><script>` objective payload confirmed rendered as inert visible text | Python 3, stdlib only, Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-08-17 | D | ambitious | Maple Press | Python CLI + self-contained dark-mode HTML dashboard that turns a CSV of Canadian businesses into ready-to-publish Canada List editorial copy — four piece types (single-business `spotlight`, ≥3-business `gift_guide`, ≥2-business "buy Canadian instead" `swap_it`, ≥2-business `local_spotlight` by province), each with a real minimum-business eligibility rule, crossed against three tones (`consumer`/`editorial`/`social`) under a genuine compatibility rule (`social` short-form is only valid for `spotlight`/`gift_guide`) and four occasions (`general`/`holiday`/`canada-day`/`back-to-school`, an explicit flag rather than auto-detected from the system clock, keeping every code path deterministically testable). Deterministic headline selection from a per-piece-type formula bank picks the least-overlapping option against a from-scratch Jaccard token-overlap novelty scorer run over a persistent, append-only local SQLite library (hand-cross-checked reference example included), so repeat generations on the same category don't keep producing the same draft. Reads Provenance/CanFile-compatible `verdict`/`confidence`/`evidence` columns when present — unverified businesses always carry an explicit "confirm before publishing" disclaimer rather than a silent claim, never suppressed even when evidence text exists for a non-canadian verdict. Optional Claude Haiku prose polish sends only the deterministically-assembled draft (never raw CSV rows), with an unconditional fallback verified to make zero network calls without `ANTHROPIC_API_KEY`. Ships a companion Claude Code Skill (`skill/SKILL.md`). First build to generate Canada List editorial content rather than verify or QC it — closes the loop after three prior builds (CanFile, Ingest Gate, Provenance) built the verification/data-quality side of the same pipeline; Category D's backlog held zero pending rows, so this was freshly generated over a "Workshop Architect" alternative (logged to `builds/ideas.md`). Manually verified end-to-end against a real 8-business fixture (mixed verdicts, not just synthetic): a live run caught and fixed a genuine bug the initial 90-test suite's first pass didn't — an unverified business with Provenance-style "uncertain" evidence text was getting a misleading "Why it's Canadian" line instead of the disclaimer, fixed by checking `verified` before `evidence` in the card-assembly priority order; re-verified live that novelty scoring changed the headline (and score, 0.00 → 0.87) on a second same-category generation against real SQLite history, that `--ai-polish` made zero network calls with no key set, and in headless Chromium (global Playwright install driving the container's pre-installed Chromium) that a `</script><script>alert(1)</script>` business-name payload and an `<img onerror>` description payload produced zero dialogs, zero page errors, and zero injected `<script>`/`<img>` elements — only the 2 script tags this build itself authored were present | Python 3, stdlib only, Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-08-18 | E | ambitious | Voxel Lab | Interactive browser trainer for the fMRI preprocessing pipeline and the multiple-comparisons ("dead salmon") problem — a Pipeline tab walks through 6 canonical steps (Motion Correction, Slice Timing, Spatial Normalization, Spatial Smoothing, HRF Convolution & GLM, Statistical Thresholding), each with a live Canvas 2D before/after visual computed from synthetic data (the Smoothing step runs a real box-blur convolution over generated noise; the HRF/GLM step runs real HRF convolution and a from-scratch least-squares fit, reporting true-vs-recovered beta); a Multiple Comparisons Lab tab generates pure-noise voxels (no true signal anywhere) and applies all four correction methods (none/Bonferroni/Benjamini-Hochberg FDR/cluster-extent) to the *same* noise draw each trial for a fair comparison, rendering a bar chart and four side-by-side "slice" visualizations of which voxels survived; a 16-question Quiz tab mixes 10 conceptual questions with 6 "computed" ones whose correct answer and distractors are regenerated from the live statistics functions each load, never a hardcoded fact. All core math (Box-Muller Gaussian RNG, Bonferroni, Benjamini-Hochberg step-up FDR, 4-connected cluster labeling, double-gamma HRF, discrete convolution, Gauss-Jordan least-squares) implemented from scratch and cross-checked against hand-worked reference values in tests. Zero external API calls or dependencies of any kind — the only build in the catalog with no live-data design tradeoff to document, since the entire tool is self-contained synthetic simulation. Day-of-year rotation (day 230 → Category E); the backlog held zero pending Category E rows, so this was freshly generated — the five prior Category E builds (Power Lab, CircuitLab, Bayes Lab, Signal Detection Lab, Portfolio Lab) are all general statistics or static anatomy, and none touch neuroimaging analysis methodology despite PROFILE.md explicitly naming "neuroimaging methods" as a rabbit-hole interest and "conduct neuroimaging and behavioral studies" as a literal day-to-day duty — a real, until-tonight-untouched gap. Manually verified end-to-end in headless Chromium: a live run at 5,000 voxels/50 trials showed uncorrected mean false positives of 254.34 versus 0.06 (Bonferroni)/0.08 (FDR)/0.42 (cluster-extent) — a concrete, visually obvious demonstration of the tool's core teaching claim; a real bug (a stray `dataset.testid` reassignment breaking the feedback element's own selector after the first quiz answer) was found via 3 failing Playwright tests and fixed; a second real bug (the Statistical Thresholding step's square voxel grid only filling half of its rectangular canvas) was found during manual screenshot QA and fixed by centering the grid; zero dialogs, zero page errors, and zero external network requests confirmed across a full session exercising every tab | Vanilla HTML/CSS/JS (classic scripts, no ES modules, opens directly via `file://`), native Canvas 2D, Playwright | complete | — | — |
| 2026-08-19 | F | ambitious | Effort Ledger | Python CLI that audits a grant budget CSV and an effort-commitment CSV for the two error classes a grants office usually catches late — a deterministic Modified Total Direct Cost (MTDC) engine (configurable fully-exempt categories, a configurable subcontract exemption threshold applying only to each subcontract's first $25k by default) cross-checks a stated `Indirect` line against the computed expected F&A cost within a dollar tolerance, alongside missing-Fringe-Benefits, zero/negative-cost, duplicate-line, and unknown-category checks; a from-scratch interval-sweep algorithm finds every contiguous date window where one person's concurrent effort commitments across multiple grants exceed a configurable cap (default 100%), reporting the exact window, peak percentage, and every contributing grant, alongside single-line-over-100%, invalid-period, grant-name-mismatch, and orphan-grant checks. Renders a self-contained dark-mode HTML dashboard (hero stats, sortable/searchable budget and flags tables, a Canvas 2D per-person effort timeline with overcommitment windows highlighted in red) plus a terminal summary and two annotated CSV exports with an appended `Flags` column. Optional Claude Haiku narrative briefing is built exclusively from an aggregate-only counts object — never a name, dollar figure, or grant ID — with an unconditional deterministic-template fallback making zero network calls with no `ANTHROPIC_API_KEY` set. Category F's 27%-chance lottery drew idea #1 (Canada List CSV Quality Inspector, written 2026-06-06), but that idea's description turned out to be a near-verbatim duplicate of the already-built Ingest Gate (2026-08-10); rather than build a duplicate, idea #1 was marked `skipped` with a note explaining the supersession and fresh Category F ideas were generated instead, landing on this build to target PROFILE.md's named "Grant writing" and "Research administration" friction points — real gaps no prior build had covered (GrantScope searches for funding opportunities; Panel Prep critiques proposal prose; Protocol Forge checks IRB ethics compliance; none audit budget arithmetic or cross-grant effort commitments). Manual end-to-end verification on the bundled 3-grant/3-person sample data caught and fixed a real bug the 43 initially-passing tests had missed: because `budget.csv` and `effort.csv` both start row numbering at row 2, the CLI's annotated-CSV writer was building one merged row-number-to-flag-codes map and reusing it for both files, so a budget-only flag on budget row 7 leaked onto an unrelated effort row 7 in `effort_flagged.csv`; fixed by keeping budget and effort flag lists separate through the CLI, with a dedicated regression test added (44 tests passing after the fix). Also live-verified in the pre-installed headless Chromium: sort/search/severity-filter interactions all worked correctly against real audit output, zero page errors/dialogs, and a `</script><script>` + `<img onerror>` payload injected into every text field (person name, grant name, grant ID, AI briefing) rendered as inert literal text with zero of the injected globals ever firing — confirming the `<` → `<` JSON-escaping added to the report renderer holds under a real HTML parser, not just a string-based unit test | Python 3, stdlib only, Anthropic API (optional, via urllib), pytest | complete | — | — |
| 2026-08-20 | G | ambitious | Fairway Physics | Browser golf game where a from-scratch physics engine — not a quiz, sort, or word mechanic — resolves every shot: per-club carry distance vs. power, headwind/tailwind and crosswind effects (with distance-scaled lateral drift), elevation change, draw/fade shot-shape curve, post-landing roll (varies by lie), lie classification (fairway/rough/bunker/water/OB/green) via a hazard-beats-green-beats-corridor priority lookup, a stroke-and-distance water/OB penalty, and a separate putting model (power, break-aim, capture radius). 9 hand-authored holes (real yardages, dogleg corridors via two adjoining fairway rectangles, bunkers, water, elevation) render top-down on Canvas 2D with an animated flight path. Two modes: Daily Round (UTC-date-seeded wind per hole via a deterministic string-hash, one completion per UTC day, shareable emoji scorecard) and Practice (any hole, unlimited replay, optional wind shuffle). Optional "Ask the Caddie" sends only hole geometry/wind/lie numbers to Claude Haiku, with an unconditional deterministic rule-based fallback. Every one of the 7 prior Category G builds (Regex Dojo, Neurofact, Synapse Sort, Confound Hunter, Heuristic Hunt, Lexicon, Quarter Call) is a quiz/sort/word-guess/chart-guess mechanic on text or chart content; this is the first Category G build driven by a real simulation engine instead, and the first build in the catalog to touch golf despite PROFILE.md naming it explicitly under both "Physical activities" and personal interests. Selected via fresh generation after Category G's 2-idea backlog (both investing-guessing games, and Quarter Call had already used real investing data in Category G nine nights earlier) missed its 25%-chance lottery draw (rolled 79). Playwright tests require `engine.js`/`course-data.js` directly under Node (the identical file the browser loads) to derive exact shot/putt inputs via a real search over the engine, rather than hand-computed "magic number" inputs. A first test run (25/26 passing) caught a real bug: the penalty branch's "Penalty!" message was being wiped in the same synchronous tick by `updateHoleInfoDisplay()` unconditionally clearing it — fixed by moving the clear to hole-load time and the non-penalty shot path. A manual QA screenshot pass then caught a second real bug the automated suite hadn't checked for: `drawCourse()` painted zones in course-data array order, so the broader `rough` rectangle (listed after `fairway`) always painted over the narrower fairway strip on every hole — the same "broad zone beats narrow zone" hazard `classifyLie` had already been fixed to handle via priority lookup, but the rendering code still used naive array order. Fixed by drawing in a fixed hazard-over-corridor priority regardless of array order, and added a pixel-level regression test (`ctx.getImageData`) since none of the prior 26 tests checked actual rendered pixels | Vanilla HTML/CSS/JS (classic scripts, no ES modules, opens directly via `file://`), native Canvas 2D, Anthropic API (optional, direct browser call), Playwright | complete | — | — |
| 2026-08-22 | I | ambitious | Renewal Radar | Python CLI + self-contained dark-mode HTML dashboard that tracks domain registrations, SSL/TLS certificates, and manually-tracked admin renewals (licenses, insurance, subscriptions, memberships) for a solo founder running multiple live platforms. `sync` performs two genuinely live, no-auth checks per monitored domain: an RDAP lookup (the modern, structured replacement for WHOIS) via the official IANA bootstrap registry to resolve each domain's authoritative RDAP server and read its registration expiration, and a direct TLS handshake (stdlib `ssl`/`socket`, no external API) to read the live certificate's expiration off the wire — one domain's failure never aborts the rest of a sync run, degrading to `Unknown` instead. Manual renewals get leap-year- and month-length-safe recurrence math (`complete` on a recurring renewal auto-schedules the next occurrence at the correct date; verified against a Feb 29 → non-leap-year annual renewal and a Jan 31 → February monthly renewal). `render` produces a dashboard with urgency-bucketed hero stats (Overdue/Due This Week/Due This Month/Upcoming/Healthy/Unknown), an Attention This Week panel, a native-Canvas-2D per-domain certificate-history chart, and a sortable/searchable table across all three source types — all dynamic data delivered via a JSON payload with `</` escaped to prevent script-tag injection and built into the DOM via `createElement`/`textContent`, never `innerHTML`. Optional Claude Haiku "Admin Briefing" sends only aggregate urgency-bucket counts and item titles/categories, with an unconditional deterministic-template fallback. Category I's backlog held zero pending rows, so this was freshly generated — chosen over a habit log (no live data source available) and a generic checklist tracker (redundant with Teamwork.com/Coda, per idea #3's existing rating notes) because it has a genuinely live, previously-untouched data source and ties directly to PROFILE.md's named "administrative overhead" friction point for someone running The Canada List and Kwyeter. Manually verified end-to-end against real domains: RDAP failed with a `403` from this build container's egress proxy (the documented, expected constraint — a normal request on the user's own machine), but the direct TLS handshake succeeded live even inside the container (port 443 sockets bypass the HTTP proxy), reading `anthropic.com`'s real certificate expiration genuinely live; `complete`/recurrence math, `list` bucketing, and `render` all verified against that real data, and live headless-Chromium QA (search filter, column sort, history-chart dropdown switch, mobile-viewport no-horizontal-scroll check) confirmed a `</script><script>alert(1)</script>` payload in a renewal title rendered as inert text with zero dialogs, zero page errors, and zero injected globals | Python 3, stdlib only, IANA RDAP bootstrap registry, live TLS (stdlib `ssl`/`socket`), Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-08-23 | A | ambitious | Trading Book | Python CLI + self-contained dark-mode HTML dashboard giving a live view of the user's real Interactive Brokers account — the first build in the catalog to connect to a brokerage account rather than public market data or synthetic teaching data, despite IBKR being named twice in PROFILE.md as a daily tool and credentialed data source. `sync` connects to a locally running TWS/IB Gateway instance via `ib_insync` and pulls real account summary (net liquidation, cash, unrealized/realized P&L, buying power) and every open position (symbol, security type, currency, exchange, quantity, avg cost, market price/value, unrealized P&L), persisting one snapshot per UTC day to local SQLite (same-day re-syncs update in place rather than duplicating, matching this catalog's established snapshot pattern). `render` builds a dashboard with red/green hero stats, a net-liquidation trend line and asset-class allocation donut (both Chart.js 4.4.4 with a verified DOM-table CDN-blocked fallback), and a sortable/searchable positions table. Optional Claude Haiku "Portfolio Note" is built from an aggregate-only prompt (day-over-day % change, allocation percentages, top movers by % change) — never a dollar figure or account ID — with an unconditional deterministic-template fallback verified to make zero network calls with no `ANTHROPIC_API_KEY`. This session's `.claude/settings.json` denies `Bash(pip install:*)`, and no third-party package (not even `yfinance`, used by several prior builds) was installed in this container — discovered and worked around by importing `ib_insync` lazily inside the one function that needs it, so `import src.ibkr_client` succeeds with or without the package installed, and every test injects a fake `ib_insync` module via `sys.modules` rather than requiring the real one. Category A's 2-idea backlog lottery (27% chance, rolled 66) missed; a stale duplicate (idea #5, a verbatim repeat of the already-built 2026-06-21 GitHub Repository Health Scorecard) was caught and corrected to `skipped` before the draw. Manually verified end-to-end: seeded a real SQLite database with 7 days of fixture history including two position symbols carrying `</script><script>...</script>` and `<img onerror=...>` payloads, then live in the container's pre-installed headless Chromium (global npm Playwright 1.56.1) confirmed zero page errors/dialogs, `window.Chart` genuinely `undefined` (CDN blocked) with the fallback tables rendering correctly, both payloads appearing as literal text in the positions table with `window.__xss`/`__xss2` never set, and exactly the 3 `<script>` tags this build itself authored on the page | Python 3, stdlib only, `ib_insync` (runtime, local TWS/IB Gateway), Anthropic API (optional, via urllib), SQLite, Chart.js 4.4.4, pytest | complete | — | — |
| 2026-08-24 | B | ambitious | Lecture Loom | Python CLI that batch-converts a folder of raw, inconsistently-formatted lecture notes into a consistent slide outline + student handout per lecture, built around a deterministic engine as the load-bearing feature rather than an AI-rewrite wrapper: a word-count-based timing model (configurable words-per-minute and target-minutes, ±10% budget tolerance) flags lectures likely to run over their time slot and names the longest section; a section-density outlier check flags sections with more than 2x the lecture's own mean bullet count; and an objective-completeness check (via an explicit "Objectives" heading block or a "By the end of this lecture, students will..." sentence pattern, both deterministic regex extraction) flags lectures with missing or sparse objectives. Ships `check` (terminal-only sanity pass), `format` (writes `<name>.outline.md`/`<name>.handout.md` per lecture), and `render` (a self-contained dark-mode HTML batch dashboard — hero stats, sortable/searchable/click-to-expand lecture table, JSON-in-`<script>` delivery with `</` escaped against premature script-tag termination, DOM built via `createElement`/`textContent` only). Optional Claude Haiku polish (`--ai-polish`) sends only the already-extracted structure (title/headings/bullets, never raw file content or personal data) to rewrite bullets into cleaner presenter phrasing and draft 2-3 discussion questions, with an unconditional deterministic whitespace/capitalization fallback making zero network calls without `ANTHROPIC_API_KEY`. Ships a companion Claude Code Skill (`skill/SKILL.md`). Selected via the Category B lottery (25% chance, rolled 2; weighted 4/10 draw against the backlog's other pending idea, a multi-repo dependency auditor) — the drawn idea's own 2026-08-15 Rating Notes had flagged it as sharing AI Lecture Builder's (2026-06-24, 2/10) "no deterministic core of comparable weight to the AI call" failure mode, so this build deliberately made the timing/objective/density engine the core feature and the AI layer optional polish on top, directly targeting PROFILE.md's named "Course material creation" friction point and the three specific courses currently taught. Manually verified end-to-end live in headless Chromium (global npm Playwright 1.56.1) against 4 fixture lectures plus a 5th carrying a live `</script><script>window.__xss=true;</script>` title and `<img onerror>` bullet payload: zero page/console errors, zero dialogs, both payloads confirmed inert, search-filter and click-to-expand-detail both exercised correctly, and zero horizontal overflow at a 375px mobile viewport; a hand-computed 10-words-at-10-wpm reference case confirmed the timing math independently of the unit tests | Python 3, stdlib only, Anthropic API (optional, via urllib), pytest | complete | — | — |
| 2026-08-25 | C | ambitious | Grant Vault | Python CLI that mines the user's own past grant documents (plain text/Markdown, single file or a folder) into a personal, searchable knowledge base of reusable grant-writing language — deterministic paragraph chunking, a heading-plus-keyword-signature section classifier across 7 named grant sections (Specific Aims, Significance, Innovation, Approach, Broader Impacts, Data Management Plan, Budget Justification), a fully deterministic 0-10 reusability scorer (rewards a portable 40-250 word length and generic/transferable language, penalizes chunks anchored to dollar amounts, calendar years, or named-entity-like phrases — every rule hand-computed and unit-tested at each tier boundary), and stdlib TF-IDF-style corpus-wide rarity-weighted tagging. Content-hash-based incremental SQLite ingest skips unchanged files and safely replaces a changed file's stale chunks. Ships `ingest`, `search` (ranked, filterable by section/tag/min-reuse), `stats`, and `render` (a self-contained dark-mode HTML dashboard — section tabs, live search, tag filter chips, copy-to-clipboard, all chunk data delivered as JSON-in-`<script>` with `</script` escaped against premature tag termination and the page built exclusively via `createElement`/`textContent`, never `innerHTML`). Optional `--ai` enrichment sends only the chunk's own already-written text to Claude Haiku for a one-sentence summary and suggested tags, with zero network calls end-to-end when the flag is unset or no `ANTHROPIC_API_KEY` is present. Fresh idea (no lottery draw — 2 pending Category C ideas, both unrated, 25% lottery chance, rolled 68) chosen to fill a real, PROFILE.md-named gap ("Grant writing" under manual-friction points) left untouched by every other Category C build (Investment Thesis Journal, Paper Lens, PubMed Research Radar, Connectome, CanFile, Citation Vault, Waymark, Curriculum Atlas) and by the compliance-focused Protocol Forge / discovery-focused GrantScope / audit-focused Effort Ledger builds. Manually verified end-to-end live in headless Chromium (global npm Playwright 1.56.1) against the two fixture grant documents plus a directly-inserted chunk carrying a live `</script><script>window.__xss_fired=true;</script>` payload: zero console errors, the injected script never executed, exactly the page's own 2 `<script>` tags present (no third node created from chunk data), the payload rendered as inert visible text, and the live search box correctly filtered results | Python 3, stdlib only, Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-08-27 | E | ambitious | Regression Lab | Interactive browser trainer for OLS regression and its diagnostic assumptions — drag points on a Canvas 2D scatterplot and watch the fitted line, R², and coefficients update live; a Diagnostics tab renders the residuals-vs-fitted, Normal Q-Q, and leverage-vs-Cook's-Distance plots for that same dataset alongside a real Breusch-Pagan heteroscedasticity test and a RESET-style quadratic-term linearity test; a Multicollinearity Lab tab uses a correlation slider between two predictors to demonstrate live that VIF and both coefficients' standard errors explode as correlation rises while joint R² stays roughly flat; a 12-question quiz mixes 4 live-diagnosed real-dataset questions (correct answer computed fresh from the same math, never a hardcoded per-preset label) with 8 fixed conceptual questions. One general multiple-regression engine (OLS via Gauss-Jordan matrix inversion) underlies everything — simple regression, the quadratic RESET test, and the 2-predictor multicollinearity demo are all the same function called differently. All core math (regularized incomplete beta for the Student's t CDF, Acklam's normal-quantile algorithm, Cook's Distance) was cross-checked against hand-worked/textbook values before UI wiring: a textbook 5-point regression example reproduced b0=2.2/b1=0.6/R²=0.6 exactly, t two-tailed p-values matched standard critical-value tables to 2 decimals, and the t-distribution at df=1 reduced exactly to the closed-form Cauchy CDF. Won the Category E lottery (25% gate rolled 13, weighted draw rolled 2/10 against idea #19, Attention Mechanism Visualizer) — the first Category E build to cover OLS regression diagnostics, a gap left by the six prior Learning Aid builds (Power Lab, CircuitLab, Bayes Lab, Signal Detection Lab, Portfolio Lab, Voxel Lab). Preset-dataset tuning surfaced a genuine finding during the build: the standard 4/n Cook's-Distance rule-of-thumb can flag a point on authentically well-behaved synthetic data by a small margin purely by chance, so the quiz's live classifier uses a dominance-ratio check (top Cook's D vs. runner-up) rather than a bare threshold crossing to avoid misreading that as a real outlier. Manually verified end-to-end in headless Chromium (global npm Playwright 1.62.1, installed fresh via `npm install` in this build folder — `@playwright/test` was not already available globally, unlike the `playwright` CLI package): 375px mobile viewport showed zero horizontal overflow, a mocked malicious Anthropic API response (`</script><script>` plus an `<img onerror>` payload) rendered as inert text with zero script execution and exactly the page's own 5 `<script>` tags present, and zero network requests occurred anywhere in a full multi-tab interaction pass with no API key set | Vanilla HTML/CSS/JS (classic scripts, no ES modules, opens directly via `file://`), native Canvas 2D, Anthropic API (optional, direct browser call), Playwright | complete | — | — |
| 2026-08-28 | F | ambitious | EDGAR Lens | Python CLI that pulls a personal watchlist's real, multi-year financial statement history (revenue, net income, operating income, assets, liabilities, equity, cash) from the free, no-auth SEC EDGAR XBRL `companyfacts` API via a documented tag-resolution layer that tries alternate US-GAAP tags per filer (since not every company reports the same line item under the same tag), restricted to annual 10-K/FY facts with a duration-bounds check that rejects stub periods and a latest-filed tie-break for restatements; computes deterministic ratio trends (net/operating margin, debt-to-equity, revenue YoY) and flags 5 fixed-threshold anomalies (revenue decline ≥10%, margin compression ≥5pp, leverage spike ≥0.5x, negative equity, swing to loss) entirely independent of any AI call. Local SQLite persistence deduplicated by (CIK, fiscal year), upserting rather than duplicating on re-sync. Renders a self-contained dark-mode HTML dashboard (hero stats, cross-company latest-FY comparison table, per-company Chart.js 4.4.4 trend chart with a verified DOM-table CDN-blocked fallback, an Anomalies panel) with an optional Claude Haiku one-sentence narrative per flagged anomaly built strictly from the already-computed aggregate numbers — never raw filing text — with an unconditional deterministic-template fallback making zero network calls with no `ANTHROPIC_API_KEY`. First build in the catalog to use SEC EDGAR, a data source named explicitly in PROFILE.md's Data Sources but never previously used across 111 prior builds despite two backlog rows proposing it; Category F's 3-idea backlog lottery (25% chance, rolled 29) missed, so this was freshly generated. All extraction logic was verified against a hand-computed fixture (4 fiscal years, a duration-bounds stub-period trap, a mixed-in 10-Q fact, and a restated fact) before any threshold was tuned, and the SEC-required `User-Agent` header defaults to a generic non-personal placeholder rather than a hardcoded real email, per STANDARDS.md. A live headless-Chromium QA pass (global npm Playwright 1.56.1, installed to a scratch directory outside the build folder since this is a Python-only build) against a rendered dashboard seeded with a live `</script><script>window.__xss=true;</script>` company-name payload confirmed zero page errors beyond the genuinely-blocked Chart.js CDN request, zero dialogs, `window.__xss` never set, and exactly the page's own 3 `<script>` tags present; that same pass caught and fixed a real mobile-viewport bug the 81 automated tests hadn't checked for — a native `<select>` sizing itself to its longest option text pushed the page 16px past a 375px viewport, fixed with `max-width`/`text-overflow: ellipsis` on the select and re-verified live | Python 3, stdlib only, SEC EDGAR XBRL API, Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-08-29 | G | ambitious | Zebra Lab | Browser logic-grid deduction game in the classic "Zebra Puzzle" tradition, but built from a from-scratch constraint-satisfaction backtracking solver (domain-filtering with standard all-different hidden-single/naked-single propagation plus generic AC-3-style arc-consistency for 4 clue relation types — eq/neq/adjacent/less — and MRV branch selection) rather than any pre-written puzzle bank; the generator picks a random solution, greedily adds true clues until the solver proves uniqueness, then runs a minimality pass that strips every clue that isn't load-bearing, so every puzzle is provably solvable by pure deduction before it's ever shown to a player. Puzzles are drawn from a real research-methods taxonomy (Population, Study Design, Confound Control, Threat to Validity, Sample Size) across 3 difficulty-gated chapters (4×3 Intro, 5×4 Standard, 5×4 Expert with an extra pruning pass), plus a UTC-date-seeded Daily Challenge (one completion per day, puzzle-content-free shareable result string) and persistent localStorage stats/streaks. Optional post-solve AI explainer composes a deterministic, factually-reviewed methodology explanation with an opt-in direct-browser Claude Haiku polish layer (session-only API key, never touches localStorage). First build in Category G driven by real logical deduction rather than a quiz, sort, word-guess, chart-guess, or physics-simulation mechanic — the prior 8 G builds (Regex Dojo, Neurofact, Synapse Sort, Confound Hunter, Heuristic Hunt, Lexicon, Quarter Call, Fairway Physics) cover all of those shapes but none do CSP-based deduction. Selected via fresh generation after Category G's 4-idea backlog lottery (25% chance, rolled 50) missed; realizes backlog idea #22 ("Research Design Deduction"), passed over twice before tonight. A first Node-level sanity sweep of the solver caught two real bugs before any UI was built: an initial naive position-major backtracking solver combinatorially exploded on the 5×4 chapters (Chapter 1 fine at 276ms/20 trials, Chapter 2/3 hanging indefinitely), and after rewriting it as a proper constraint-propagation solver, the all-different propagation's two elimination loops were found to be transposed no-ops (each was clearing a cell that the very condition triggering it had already guaranteed was clear) — fixed, then verified at 1.6s/2.5s for 20 full trials (generation + uniqueness proof + complete per-clue minimality verification) each for Chapters 2/3 | Vanilla HTML/CSS/JS (classic scripts, no ES modules, opens directly via `file://`), Anthropic API (optional, direct browser call), Playwright | complete | — | — |
| 2026-08-30 | H | ambitious | Layer Guard | Python CLI that parses any Python codebase's import statements via `ast` (never executing scanned code) and builds a first-party module dependency graph, then runs a from-scratch iterative Tarjan's strongly-connected-components algorithm to detect real circular-import cycles with exact file:line evidence for every edge in each cycle; computes per-module afferent/efferent coupling and a Ce/(Ce+Ca) instability score, flagging modules that are both heavily depended-upon and highly unstable as "structural risk" (a Stable Dependencies Principle violation); and, given an explicit `layers.json` ordering, flags every import that reaches from a lower/more-stable declared layer up into a higher/less-stable one, with unassigned modules reported separately rather than silently misclassified. Renders a terminal summary (exit code 2 on any cycle, for CI gating), `--json`, or a self-contained dark-mode HTML dashboard (hand-drawn Canvas 2D dependency graph colored by layer with cycle/violation edges highlighted, sortable/searchable metrics table, all dynamic data delivered via a `application/json` script tag with `<` escaped to prevent premature tag termination and the DOM built exclusively via `createElement`/`textContent`). Optional Claude Haiku refactor-advice note is built strictly from aggregate structure (cycle chains as module-name lists, violation tuples, top-risk module names/metrics -- never file paths or source text), with an unconditional deterministic-template fallback verified to make zero network calls with no `ANTHROPIC_API_KEY`. Category H's one pending backlog row (idea #9, GitHub Actions Performance Analyzer) turned out to be the same verbatim duplicate of the already-built `ci-pulse` (2026-06-28) that the 2026-08-12 Snipvault build had already corrected to `skipped` once before -- that fix never reached `main` since none of this repo's ~30 open nightly-build PRs have ever merged, so every session forks fresh from the same stale, uncorrected `main`; corrected again here, emptying the pool and routing to fresh generation. First build in the catalog to analyze this repository's own (or any Python project's) internal import architecture, directly targeting PROFILE.md's verbatim-named "Better understand scalable software architecture" learning goal and its explicitly-named "Overly complex architectures" thing-to-avoid, both untouched across the prior 77 builds. Manually verified end-to-end against this build's own real codebase (not just fixtures): 9 modules, 14 edges, 0 cycles, 0 layering violations against the committed `layers.example.json` describing its own architecture, and 1 correctly-unassigned module (the package's own empty `__init__.py`) -- confirming the tool's own design is genuinely acyclic and correctly layered; also ran live against a real 3-module synthetic cycle fixture through the actual CLI entry point (not just mocks), correctly detecting `cyclepkg.a -> cyclepkg.b -> cyclepkg.c -> cyclepkg.a` with accurate evidence and exit code 2, and confirmed zero network calls end-to-end with `urllib.request.urlopen` monkey-patched to raise if ever called and no `ANTHROPIC_API_KEY` set | Python 3, stdlib only, Anthropic API (optional, via urllib), pytest | complete | — | — |
| 2026-09-01 | A | ambitious | Fleet Drift | Python CLI that lists every GitHub repo the user's own `GITHUB_TOKEN` owns, reads `requirements.txt`/`package.json` from each repo's default branch, and looks up each dependency's real latest version on the free PyPI JSON API and npm registry API — the differentiating layer is a cross-repo drift matrix (from-scratch semver parse/compare/classify, no third-party library) that flags every dependency pinned to 2+ distinct versions across 2+ repos, severity-ranked patch/minor/major, alongside a separate per-repo staleness rollup against each dependency's true latest release. Local SQLite snapshot persistence (same-UTC-day re-sync upserts, later days accumulate real drift-over-time history, matching this catalog's established pattern). Renders a self-contained dark-mode HTML dashboard (hero stats, sortable/searchable drift matrix, a Chart.js 4.4.4 most-drifted-dependencies bar chart with a verified DOM-table CDN-blocked fallback, per-repo staleness panel; all dynamic data delivered via an escaped JSON payload, DOM built exclusively via `createElement`/`textContent`). Optional Claude Haiku "what to fix first" briefing built strictly from aggregate dependency names/severities (never file contents), unconditional deterministic-template fallback verified to make zero network calls with no `ANTHROPIC_API_KEY`. Missed Category A's 27%-chance backlog lottery (rolled 58 of 4 pending, mostly-unrated rows); fresh generation directly realizes a critique already recorded in this repo's own backlog (idea #14, 2026-08-15: the already-built `dep-check` audits one repo in isolation and was passed over for a second build "only if paired with something dep-check doesn't do — e.g. cross-repo shared-dependency-version drift detection", exactly this build's core mechanic), targeting PROFILE.md's verbatim-named "Managing many simultaneous projects" and "Keeping multiple data systems synchronized" friction points. This build container's Bash tool denied outbound `curl` to `pypi.org`/`registry.npmjs.org`/`api.github.com` outright (not even a 403), confirming CLAUDE.md's documented network constraint; every test injects a fake `urlopen` transport instead. A first full test run hung at ~5.8GB RSS/99.9% CPU — caught and fixed a real bug in the test fixtures themselves (not the shipped code): a fake-transport URL check for `"page=1"` false-matched the `per_page=100` query parameter that's always present, causing GitHub pagination to never terminate and the accumulated repo list to grow forever; fixed by checking `"&page=1"` instead. Manually verified end-to-end (not just mocks): seeded a realistic 6-row, 4-repo SQLite fixture directly (bypassing the build container's network restriction, consistent with this catalog's established workaround) and ran the real `list`/`render`/`render --ai`/`history` CLI commands against it — `list` correctly reported a MAJOR-severity `django` drift (3.2.0 vs 5.1.0) and a MINOR-severity `requests` drift (2.28.0 vs 2.32.3) while correctly leaving a range-pinned, single-repo `vite` entry unflagged; the rendered dashboard's embedded JSON payload matched the seeded data exactly; `render --ai` with no `ANTHROPIC_API_KEY` returned the deterministic fallback briefing instantly with no network attempt | Python 3, stdlib only, PyPI JSON API, npm registry API, GitHub REST API (`GITHUB_TOKEN`, runtime), Anthropic API (optional, via urllib), SQLite, Chart.js 4.4.4, pytest | complete | — | — |
| 2026-09-02 | B | ambitious | CiteForge | Python CLI that converts a bibliography (BibTeX file, DOI list via the free Crossref API, or free-text reference lines) into a correctly-formatted reference list in APA 7, AMA 11, Vancouver/ICMJE, or Chicago Author-Date 17 style, or all four side by side — the differentiating layer is a from-scratch citation-formatting engine implementing each style's real rules (not template-filling): per-style author-list "et al." thresholds (APA 20/19, AMA/Vancouver 6, Chicago 10/7), sentence-case/title-case converters that preserve acronyms and internally-capitalized words, and an ICMJE page-range digit-elision algorithm (`284-287` → `284-7`) cross-checked in tests against a real published ICMJE sample citation, which the tool reproduces to the letter. A local SQLite library dedupes by DOI (or normalized author+year+title) so repeat imports upsert rather than duplicate, and caches every resolved DOI so re-syncs never re-fetch. Free-text reference parsing runs a deterministic regex pre-pass first; only lines it can't confidently structure optionally go to Claude Haiku (`--ai`, requires `ANTHROPIC_API_KEY`), with unresolved lines flagged `needs_review` rather than guessed. Renders a self-contained dark-mode HTML comparison report (per-entry per-style copy buttons, live search, JSON-in-script-tag delivery with `</` escaped, DOM built exclusively via `createElement`/`textContent`) and ships a companion Claude Code Skill (`skill/SKILL.md`). Category B's single pending backlog idea (#14, a multi-repo dependency batch auditor) turned out to be a stale duplicate of the immediately-preceding Fleet Drift build and was corrected to `skipped`, emptying the pool and routing to fresh generation; targets PROFILE.md's named "write grants and manuscripts" duty and "Literature reviews" friction point, a gap neither existing citation-adjacent build (Citation Vault: reading ledger; Manuscript Pipeline: submission tracking) had touched. Manually verified end-to-end (not just mocks) against a real, independently-verifiable published APA reference (Grady et al., 2019, *Psychology of Popular Media Culture*) — the tool's APA output matched the real published citation exactly except for one documented, expected limitation (a proper noun inside the title, "United States," gets lowercased by the acronym/internal-capital-only sentence-case heuristic, which has no name dictionary) — and live in headless Chromium (playwright 1.56.1 installed to a scratch directory, consistent with this catalog's established pattern for Python-only builds needing a one-off browser check): zero page errors, zero dialogs, a `</script><script>window.__xss=true;</script>` payload in a reference title confirmed to never execute (`window.__xss` never set) and render as literal text in all 4 style blocks, live search filtering verified, and a 375px mobile viewport showed zero horizontal overflow | Python 3, stdlib only, Crossref API, Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-09-03 | C | ambitious | Promptbook | Python CLI that turns the prompts a user has already typed into Claude Code into a searchable, scored, zero-manual-entry knowledge base — recursively scans local Claude Code session transcript files (`~/.claude/projects/**/*.jsonl`, or `CLAUDE_CONFIG_DIR`/`--claude-dir` override), extracts every genuinely human-authored prompt turn (excluding tool-result echoes and subagent sidechain turns, verified against this build container's own real transcript format before writing the parser), and for each one deterministically derives what happened next in that same session — files edited, a test-runner command run with a pass/fail signal, a git commit, an unresolved error — into a from-scratch 0-10 effectiveness score (commit +4, passing tests +3, ambiguous test signal +1, clean edit +2, unresolved error -3, clamped) and a keyword-heuristic task-type tag (bug-fix/feature/refactor/research/test/docs/config/review/other). Local SQLite library deduplicated by prompt UUID with incremental per-file line-count tracking so re-ingesting never re-scans or duplicates unchanged sessions. Ships `ingest`/`search`/`stats`/`render` (a self-contained dark-mode HTML dashboard, JSON-in-script-tag delivery with `</` escaped, DOM built exclusively via `createElement`/`textContent`), an optional Claude Haiku "why this worked" one-sentence note on top-scoring prompts built strictly from the prompt text and already-computed aggregate features — never raw tool output — with an unconditional deterministic-template fallback verified to make zero network calls with no `ANTHROPIC_API_KEY`, and a companion Claude Code Skill (`skill/SKILL.md`). Category C's 4-idea backlog lottery (25% chance, all four ideas unrated) missed on a roll of 92, routing to fresh generation; chosen over Highlight Vault (idea #28, rejected as sitting too close to Citation Vault/Connectome per its own prior note, and still manual-entry-dependent) and a Canadian Open-Data Consumer Knowledge Base (logged as new idea #41, reads closer to a Category F build) because it directly targets two PROFILE.md-named friction points ("Context loss between AI coding sessions", "Re-establishing context across AI sessions") with zero manual entry, a gap none of the 9 prior Category C builds touch. Manually verified end-to-end against this build container's own real, live session transcript (not just fixtures): `ingest` correctly extracted exactly 1 genuine human prompt out of ~100 raw JSONL lines, scored it 5/10 (test-pass + clean-edit signals, both genuinely present in this very session), and every stored field matched the session's real state exactly; a live `</script><script>window.__xss=true;</script><img onerror>` payload inserted into the real database and rendered in headless Chromium (global npm Playwright 1.56.1, pre-installed Chromium) confirmed zero script execution, live search filtering narrowing from 2 cards to 1 to a correct empty state, zero page errors/dialogs, and zero horizontal overflow at a 375px mobile viewport | Python 3, stdlib only, Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-09-04 | D | ambitious | CaseForge | Python CLI that generates classroom-ready teaching case vignettes from real, live PubMed research findings — `esearch`/`efetch` (free, no-auth NCBI E-utilities) resolve a course topic into real published abstracts, a deterministic regex/keyword extraction layer pulls sample size, effect size, p-value, methodology tag, and population descriptor directly from each abstract's own text, and a discussion-question rule engine (7 predicate-gated templates plus 3 always-included fallbacks) generates only the questions the extracted facts actually justify — a small-N study gets a power question, a correlational design gets a causality question, a study with no stated control group gets a confound question, and so on, deterministically and reproducibly. A deterministic template assembler builds a complete vignette from those same facts with zero API key required; an optional Claude Haiku polish pass (`--ai-polish`) may only replace it if every extracted numeric/statistical fact string still appears verbatim in the AI's rewrite, with an unconditional fallback to the deterministic text otherwise (dedicated test coverage proves a response dropping one required fact is rejected). Local SQLite library keyed by PMID so a re-`generate` over an overlapping query never duplicates an already-fetched article; ships `generate`/`list`/`show`/`search`/`export markdown`/`render` (a self-contained dark-mode HTML dashboard, JSON-in-script-tag delivery with `</` escaped, DOM built exclusively via `createElement`/`textContent`) and a companion Claude Code Skill (`skill/SKILL.md`). Category D's one pending backlog idea (#17, Workshop Architect) lost its 25%-chance lottery draw (rolled 44), routing to fresh generation; chosen over an investment pre-mortem generator (logged as idea #42 — a genuinely new shape, but investing had already appeared twice in the last 10 builds) and a Kwyeter noise-card generator (idea #43 — real project tie but thinner scope) because it targets PROFILE.md's verbatim-named "Course material creation" friction point and three specifically named courses with zero prior coverage, and deliberately breaks from the taxonomy × compatibility-rule-engine mechanism reused by three prior Category D builds (Research Question Forge's sibling pattern, Bridgework, Maple Press) by grounding its rule engine in facts dynamically extracted from live external data instead of a hand-authored taxonomy. This build container's egress proxy was confirmed live this session to block PubMed (`esearch failed: ... 403 Forbidden`), the expected/documented constraint — every test mocks the network layer, and a full manual end-to-end CLI walkthrough (`generate`→`list`→`show`→`export`→`render`) against realistic mocked PubMed data, including a script-injection payload embedded directly in a fetched article title, confirmed the rendered dashboard escapes it correctly (round-trips to the exact original string via JSON, never appears as an executable tag) with zero `innerHTML` used anywhere in the template. A real grammar bug (hardcoded "a" instead of "an" before vowel-starting population descriptors like "undergraduate sample") was found during that manual pass and fixed with a shared `indefinite_article()` helper, re-verified live | Python 3, stdlib only, PubMed E-utilities, Anthropic API (optional, via urllib), SQLite, pytest | complete | — | — |
| 2026-09-05 | E | ambitious | Mediation & Moderation Analysis Lab | Interactive browser trainer for the two most common multivariate techniques in social/affective/stress psychology research — a Mediation Lab tab (slider-controlled X→M→Y data-generating process, live path a/b/c/c′ estimation, a 2000-resample bootstrap percentile CI for the indirect effect a×b shown alongside a Sobel test, and a live path diagram) and a Moderation Lab tab (centered-interaction regression, simple slopes at ±1 SD of the moderator derived from the coefficient covariance matrix, and an exact Johnson-Neyman region-of-significance solve via the quadratic formula, rendered as a scatterplot with three slope lines plus a colored significance strip). A single from-scratch OLS engine (Gauss-Jordan matrix inversion with partial pivoting, full coefficient covariance matrix, Lanczos log-gamma/regularized-incomplete-beta Student-t distribution) underlies both tabs — no statistics or charting library. A 16-question quiz mixes 8 fixed conceptual questions with 8 questions built from a freshly generated scenario each time, whose correct answer is always derived from the same engine functions shown in the lab tabs. Optional Claude Haiku "explain this" layer sends only already-computed aggregate statistics (never raw sample rows), with an unconditional deterministic-template fallback verified to make zero network calls with no `ANTHROPIC_API_KEY`. Every core formula (OLS coefficients/SEs, the mediation identity c=c′+a×b, the Johnson-Neyman roots) was independently hand-computed via a from-scratch pure-Python Gauss-Jordan reference implementation before any JS was written, and those exact reference values are hardcoded as the ground truth in the test suite. Category E's one pending backlog idea (#19, Attention Mechanism Visualizer) missed its 25%-chance lottery draw (rolled 33), routing to fresh generation; selected over a Meta-Analysis/Forest-Plot Trainer (logged as new backlog idea #44) because two of PROFILE.md's three named courses (Stress and Coping, Social Affective Neuroscience) map directly onto mediation/moderation as their standard analytic backbone, a topic none of the seven prior Category E builds (Power Lab, CircuitLab, Bayes Lab, Signal Detection Lab, Portfolio Lab, Voxel Lab, Regression Lab) touch. First test run (44/49) caught 5 real issues — 3 in the test fixtures themselves (mislabeled coefficient indices, a step-misaligned slider value, a wrong quiz-answer-index assumption) and 2 that reflected genuine statistical properties the tests had assumed incorrectly (a zero-noise mediation sample makes X and M perfectly collinear and singular, not "tighter"; the sample-SD convention used throughout the app doesn't match the classic population-SD textbook example) — all fixed and re-verified at 49/49. Manually verified end-to-end in the container's pre-installed headless Chromium (not just the automated suite): screenshotted all three tabs plus a 375px mobile viewport with zero console/page errors, confirmed the deterministic AI-explain fallback text on both lab tabs reads correctly, and completed a full 16-question quiz run through to the score/review screen | Vanilla HTML/CSS/JS (classic scripts, no ES modules, opens directly via `file://`), native Canvas 2D, Anthropic API (optional, direct browser call), Playwright | complete | — | — |

---

## Category Key

| ID | Category |
|----|----------|
| A  | Dashboard / Visualizer |
| B  | Productivity Utility |
| C  | Personal Knowledge Tool |
| D  | Creative / Generative |
| E  | Learning Aid |
| F  | Data Explorer |
| G  | Game / Puzzle |
| H  | Developer Tool |
| I  | Life Admin Helper |

## Status Key

| Value | Meaning |
|-------|-------|
| `complete` | All hard standards met, committed and pushed |
| `partial` | Build works but scope was reduced; documented in BUILD_LOG |
| `aborted` | Could not complete safely; see ABORTED.md in the dated folder |
| `discarded` | Build completed but judged not worth keeping; folder renamed with -DISCARDED suffix |
