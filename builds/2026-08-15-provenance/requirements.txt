# Stdlib only — no third-party dependencies.
# urllib.request handles the Wikidata/Wikipedia and optional Anthropic API calls,
# sqlite3 handles the local cache/history. This file is intentionally empty.
